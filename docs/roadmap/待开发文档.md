# 待开发文档

> **定位（2026-09-14 文档清洗）**：本文件＝**需求池 / backlog（唯一需求登记入口）**，只负责"登记需求"。开发排序与实施要点见同目录 `开发工作计划.md`；当前批次路线图见同目录 `统一实施路径与任务分解-20260914.md`（**唯一计划源入口**）。索引见 `docs/README.md`。
>
> 本文件是 golden-shuttle 项目的开发需求登记处。
> - **记录**：在本对话（需求登记会话）中提交需求，由助手写入本文件。
> - **执行**：实际开发在其它对话中按本文件发起指示，不在本对话进行。
> - 每新增一条需求时，编号递增；状态变化时直接修改对应条目。

## 状态图例

| 状态 | 含义 |
| ---- | ---- |
| 待开发 | 已登记，尚未排期开发 |
| 开发中 | 已在其它对话开始开发 |
| 已完成 | 开发并验证完成 |
| 已取消 | 不再开发 |

## 需求清单

| 编号 | 日期 | 需求描述 | 优先级 | 状态 | 备注 |
| ---- | ---- | -------- | ------ | ---- | ---- |
| REQ-001 | 2026-09-07 | 仪表盘顶部数据卡片增加点击筛选交互：点击「进行中」「待配料/未开始」「已完成」任一卡片，下方「款式开发清单」同步仅展示对应状态的款式；点击「总数」或再次点击当前已选中卡片可恢复全部展示。卡片需有选中态视觉反馈，清单标题「款式开发清单（N 款）」的计数随筛选结果联动。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_001.png；「进行中」=打版中+样衣中+待确认（与统计卡计数口径一致）；筛选兼容清单既有排序/分页（当前无排序分页，无冲突）。2026-09-07 开发并验证：浏览器 E2E 16 项检查全 PASS（默认6行/各状态筛选计数联动/选中态切换/再次点击恢复/总款数恢复/零 console 错误），生产构建通过，commit 见 feature/sample-run-model。 |
| REQ-002 | 2026-09-07 | 打样需求单编辑页「图纸资料」标签页的资料卡片缩略图显示不全：竖向图片（人体效果图、成衣图等）在固定横向比例的缩略图区域被上下裁切，仅露出中间部分。期望缩略图改为自适应完整展示（完整显示图片，不足区域留白），或提供「裁切/完整」展示方式切换按钮；两种方案开发时择优实现。 | P2 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_002.png；择优实现「自适应完整展示」：DrawingLibrary 卡片网格与版本历史缩略图改 object-fit:contain（PdfThumb 传参+CSS 同步），放大预览本就 contain；非图片类占位（PRJ/EMF/DXF 图标）与矢量缩略图同步完整显示。2026-09-07 验证：E2E 检查 14 份资料缩略图计算样式全部 contain，截图目检完整。 |
| REQ-003 | 2026-09-07 | 仪表盘/款式开发清单信息展示优化，含 4 个子项：①品类占比图表（外套/连衣裙/风衣/下装）增加点击筛选，点击后清单同步展示该品类款式，交互与状态卡片筛选一致（选中态/再次点击恢复/标题计数联动）；②清单增加「版师」「样衣工」列（及对应数据字段与录入入口），直观展示款式当前在谁手里，未分配显示「未分配」；③进度展示优化：当前「当前进度」标签过于笼统（如「已完成」无法区分头版样/复板等环节），应突出「最先进批次」信息，让用户快速识别哪些款在打胚/头版样/复板等具体环节，可考虑将最先进批次提升为主进度展示或细化进度标签；④顶部统计卡分类与实际进度状态对齐：当前 4 张卡片（总数/进行中/待配料/已完成）未独立体现「待确认」状态（被归入进行中），需调整——或增加「待确认」卡片，或重构顶部统计为与开发进度分布一致的粒度。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_003a.png（无待确认）、docs/archive/req-screenshots/_req_screenshot_003b.png（有待确认，顶部卡片未独立体现）；4 子项已全部完成：①品类条点击筛选（与状态筛选可叠加）；②迁移 v13 sample_runs 加 pattern_maker/sample_maker（旧 assignee 并入版师后删），批次表单拆版师/样衣工两输入，清单新增两列（取最先进批次）；③合并「当前进度+最先进批次」为单列主进度（版次·批次状态着色）；④4 卡→5 卡加「待确认」，进行中口径=打版中+样衣中（互斥）。2026-09-07 验证：E2E 17 项全 PASS、生产构建通过。 |
| REQ-004 | 2026-09-07 | 款/版次信息归属重构（数据模型+界面）：当前「款式基础信息」「款单信息（打样单号/审核状态/看板状态）」「打样批次」混在同一张打样需求单页面，且打样单号、审核状态为页面级一成不变，与实际业务不符。期望：①款式基本信息（编号/名称/类别/品牌/设计师/年度/季节/波段）独立展示，作为款级共享载体，不依附于任一打样单页面；②打样单号下沉到版次级——每个版次（胚样/头版样/复板等）各有自己的打样单号，页面级不再挂单一固定单号；③审核按版次独立进行：审核对象是具体样衣（审胚样/审头版样/审生产样），审核状态与审版意见跟随各版次样衣独立记录，各版次意见互不影响。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_004a.png（红框款级信息/绿框款单信息/批次区）、004b.png（橙框审核意见区）；设计决策（用户 2026-09-07 拍板）：V 编号=款内批次顺序 V0 起一位、款式信息用右侧抽屉、款单看板状态保留款级。实施（迁移 v14）：①新组件 StyleInfoDrawer 右侧抽屉（款号/名称/类别/品牌/设计师/年度/季节/波段+设计稿只读），保存走新增 PUT /api/styles/:id（款级权威），入口=看板卡片 Info 按钮+详情页顶部「款式信息」按钮，详情页「款式基础信息」区块移除；②sample_runs 加 order_no=PO-{款号}-V{n}（款内顺序、删除不重排、max+1），批次卡片顶部只读展示单号，tasks.order_no 废弃清空；③sample_runs 加 audit_status（未提交/待审核/已通过/已驳回）+audit_comment，批次表单独立编辑，旧款级审核迁最先进批次，审核变更写操作日志。2026-09-07 验证：HTTP 全链路+浏览器 E2E 19 项全 PASS+生产构建通过。方案修订（用户验收）：抽屉「藏信息」反人类不接受，已移除 StyleInfoDrawer 及全部入口，恢复详情页醒目「款式信息」区块（8 字段直接展示+编辑，款级共享标注）；审版意见改多行 textarea；修订后 E2E 10/10 PASS+构建通过。 |
| REQ-005 | 2026-09-07 | 尺寸表体系重构与功能完善，含 4 个子项：①尺寸表归属版次：当前尺寸表承载一个款式多版次的数据、只对"当前单"负责，需改为每个版次拥有独立尺寸表（与 REQ-004 信息归属重构一致）；②标准/实测比对：在既有「录入成衣实测」「报警/偏差」列基础上完善功能——录入实际测量值后与标准码比对，超公差即报警并显示偏差值；③自动放码（拓码）：完善既有「拓码模式」——按部位档差自动生成各码（XS/S/M/L/XL 等）尺寸，支持放码规则维护（指令/拓码维护）；④跨版次尺寸对比：提供显性入口选择另一版次的尺寸表与当前版次对比（如头版 vs 生产样），按部位并排展示两版数值并高亮差异、显示增减量（如衣长+10cm、胸围+6cm），帮助设计师快速识别版次间改动。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_005a.png（单码表）、005b.png（录入成衣实测/报警偏差）、005c.png（拓码多码表）；方案已拍板（用户 2026-09-07 全按推荐）：①尺寸表归属版次——新批次默认从品类预设复制生成（预设无记录则空表），批次内独立编辑；②标准/实测比对——实测列输入后与同码标准比对，超公差红色报警并显示偏差值；③自动放码——档差从品类预设继承，按部位档差生成各码；④跨版次对比——同码（M 对 M）按部位匹配并排展示、差异高亮+增减量显示。实施（迁移 v16）：sample_runs 加 size_data 列，旧 tasks.size_data 迁至各款首个批次（sort_order 最小）后清空（列保留兼容）；后端 sampleRuns.cjs 加 presetSizeData（从 measurement_templates 按品类生成）、create 支持 init_size_data/显式 size_data、listByTask 解析数组，tasks.cjs attachRuns 把 task.size_data 从首个批次投影（兼容旧消费点），versions.cjs 快照含批次 size_data、diffSummary 优先批次级、rollback 按 run.id 写回；前端 SizeTable.jsx 改 compareRuns props+同码换算 compValOf（对比批次码不同按放码规则换算），DetailView.jsx 尺寸 Tab 加批次选择器+「从上一版次复制」按钮（ConfirmModal 确认→updateRun 覆盖）+导出传 run 覆盖，SampleRunList.jsx addRun 传 init_size_data。验证：E2E 10/11 PASS（批次选择/迁移承接 12 行/PATCH 保存/reload 保持/新建批次/对比栏/对比列/复制按钮/复制 PATCH；唯一 FAIL「预设生成 rows=0」根因 measurement_templates 无「外套」品类预设=正确 fallback，临时插入预设补验生成 1 行成功，测试数据已清理）+生产构建通过。2026-09-07 修订（用户验收三问题）：①新增版次后尺寸页不刷新——DetailView 提取 reloadRuns，切到尺寸 Tab 时强制重拉批次 + SampleRunList 新增/删除批次后经新 prop onRunsChanged 通知父级即时刷新（无需退出详情重进）；②「从上一版次复制」无空数据校验——点击复制先校验来源批次 size_data，空表（0 行）直接阻止并弹「无法复制：来源版次尺寸表为空」提示（原确认弹窗一并显示来源版次+行数）；③尺寸页当前版次不明确——批次选择器上方新增高亮「当前编辑版次」横幅（accent 色底，显示 order_no·版次·尺码，标注自动保存/导出/对比均以当前版次为准）。验证：E2E 8/8 PASS（含新增版次后尺寸页即时出现 2→3、空来源阻止提示、有来源确认弹窗含行数、临时批次清理恢复）+生产构建通过。2026-09-07 二次修订（用户验收，风险仍大）：①尺寸表**绑定具体版次**——移除尺寸页顶部「切换版次尺寸表」下拉，编辑页不再有随手下拉可切；当前编辑版次横幅保留（accent 高亮 order_no·版次·尺码，标注"尺寸表已绑定该版次"）；②切换编辑版次改为**弹窗明确选择**（新增 RunPickerModal 版次选择弹窗：列表显示各批次 order_no·版次·尺码·行数，选中后「切换到此版次」生效，切换有 toast 反馈）；③「从上一版次复制」改为「**从其它版次导入**」——明确的选择参照版本流程：弹窗列出除当前外全部批次（仅显示有数据批次可选，无数据标注"不可导入"并禁用）→ 选中来源 → 「下一步：确认导入」二次确认（显示来源版次+行数+覆盖提示）→ 执行导入（toast 反馈"已从 X 导入 N 行，原数据已覆盖"）。验证：E2E 8/8 PASS（无编辑下拉/横幅默认 V0/切换弹窗列出其它版次且排除当前/切换后横幅+toast 更新/导入弹窗 V0 可选 V2 无数据禁用/二次确认含来源行数/取消后数据未改动）+生产构建通过。2026-09-07 三次修订（用户验收，方向调整）：①**尺寸表入口改为批次卡片**——SampleRunList 每个批次卡片新增「尺寸表」按钮（Ruler 图标），点击进入该版次尺寸页并绑定（onOpenSizeTable → setSizeRunId+切 tab+reloadRuns）；②**尺寸页完全锁定版次**——删除弹窗式切换/导入入口（RunPickerModal 组件删除），横幅改「当前编辑版次（已锁定）」并注明"本页仅编辑该版次尺寸表、各版次数据隔离，编辑其它版次请返回基本信息页从其批次卡片进入"，页面无任何可改版次控件；③**导入并入跨版次对比**——「从其它版次导入」独立入口删除，改为在跨版次对比视图中操作：对比栏选择参照版次后，对比列并排展示两版数值+差异高亮（红色增减量），对比确认合适后点「导入该版次数据到当前版次」→ ConfirmModal 确认（来源版次+行数+覆盖提示）→ 整体导入，导入后 toast 反馈——先看数据再决定，杜绝"开盲盒"。验证：E2E 8/8 PASS（批次卡片入口/从 V0 卡片进入锁定 V0/无切换控件/对比下拉+导入按钮/确认弹窗含来源行数/取消不执行/从 V1 卡片进入锁定 V1/零 console 错误）+截图目检（对比列差异红色标注清晰）+生产构建通过。2026-09-07 四次修订（用户验收）：①**导入前可查看参考版次完整尺寸表**——对比栏新增「查看该版次完整尺寸表」按钮（Eye 图标），弹出只读弹窗展示参考版次全部数据（部位/测量方法/标准码值/各码值/放码规则/公差/备注），导入前先核对完整数据再决定，不再盲盒；②**尺寸指标 Tab 直接进入时展示最新编辑的尺寸表**——reloadRuns 未指定批次时按 updated_at 最新（其次 id 最大）选中批次并锁定展示，从批次卡片进入则仍绑定该卡片版次（二者互不干扰）。验证：E2E 5/5 PASS（Tab 直接进入展示最新批次 V2/卡片进入仍锁定 V0/查看完整表按钮/弹窗 12 行含测量方法与各码值/弹窗可关闭/零 console 错误）+弹窗截图目检+生产构建通过。2026-09-07 五次修订（用户验收）：**参考版次完整尺寸表改为当前窗口内嵌预览**（不再弹窗）——选中对比版次后，参考版次完整表（部位/测量方法/标准码值/各码值/放码规则/公差/备注，只读）直接内嵌显示在对比栏下方、主表上方，与主表同屏核对，导入按钮仍在对比栏（「导入该版次数据到当前版次」），确认弹窗提示"请确认上方参考版次完整表与差异列已核对无误"；未选对比时区块不显示。验证：E2E 4/4 PASS（未选对比无区块/选 V1 内嵌出现且无弹窗/内嵌表 12 行含测量方法与各码值/导入按钮仍在/零 console 错误）+截图目检+生产构建通过。**2026-09-07 六次修订（用户意见，未实施——用户明确"先不要改，将这条意见记录到待开发文档"）**：弹窗（四次修订）与内嵌参考表区块（五次修订）均被否决，用户定性"完全是本末倒置、搞这么复杂"。用户明确要求：**直接在目标尺寸表的「比对值」列显示参考版次尺寸**（现有跨版次对比的比对值列即为此用途——同码对照+差异增减量），**不再新增任何独立参考表**（不弹窗、不内嵌区块）；在比对值列看到参考版次数据、确认合适后，直接点「确认导入」即可。待用户确认后实施：移除 SizeTable.jsx 中 compareRun 的内嵌参考版次完整尺寸表区块，保留比对值列与「导入该版次数据到当前版次」按钮。2026-09-08 六次修订实施（用户重新拍板："在上一个版尺寸表已填好的情况下，打开新版次尺寸表是空的，可考虑复制上一版，但复制前先查看尺寸再决定是否复制，跨版次对比功能无缝衔接"）：①**移除内嵌参考版次完整尺寸表区块**（弹窗/内嵌表全部删除，Eye 图标与相关代码一并清理）；②**比对值列即查看载体**——选中对比版次后主表「比对值」列紫色显示参考版次同码尺寸+差异增减量，说明文字更新为"表格「比对值」列为 XX 尺寸数据，查看确认合适后点导入"；③**空表预览（新批次复制场景）**——当前版次尺寸表为空且选中对比版次时，表格自动以参考版次数据为预览行（浅拷贝防污染、preview-row 只读防误改、标准码留空占位"待导入"、紫色「比对值」列显示其值），顶部虚线提示条说明"当前版次尺寸表为空，表格暂显示 XX 数据预览，查看确认合适后点导入整体复制"；④导入按钮+确认弹窗（来源+行数+覆盖警示）不变。验证：E2E 6/6 PASS（V2 空表进入/预览提示条/预览 13 行（12 数据+快速添加行）标准码空比对值显示 V1 值 55/预览行只读/导入确认弹窗含来源 V1+12 行/取消后 V2 仍 0 行未动）+V0 有数据场景正常（无预览、比对值列显示 V1 值+diff +3.0/+12.0）+生产构建通过（在真实路径 D:\dev\golden-shuttle 下）。**开发日志-关键技术发现（2026-09-08）**：C:\Users\Administrator\golden-shuttle 是指向 D:\dev\golden-shuttle 的符号链接；vite 构建若从符号链接 cwd 运行，vite 以 import.meta.url 的 realpath（D 盘）推断 root，vite:build-html 报 "fileName must be neither absolute nor relative paths, received D:/dev/golden-shuttle/index.html"；**vite build 必须在真实路径 `D:\dev\golden-shuttle` 下执行**（`cd D:\dev\golden-shuttle; node node_modules/vite/bin/vite.js build`），node -e 验证 realpathSync(cwd)=D:\dev\golden-shuttle；dev 服务（5173/3001）与 git 操作经符号链接路径均正常，仅 vite build 受影响。 |
| REQ-006 | 2026-09-07 | 交互与确认机制优化，含 3 个子项：①侧边栏自动感知展开：顶部/侧边栏的收起按钮改为智能感知——鼠标靠近边缘区域时自动展开侧边栏，无需点击；②删除操作增加确认反馈：详情页各处的删除按钮（工作动态条目、打样批次等）与尺寸指标表每行的删除按钮（🗑）当前点击即删、无任何确认，需统一增加删除确认反馈（确认弹窗/二次确认），防误删；③评估「保存」「取消」按钮去留：当前数据为自动保存机制，详情页右上角「保存」「取消」按钮的存在必要性存疑，需评估是否删除——若保留需说明其不可替代场景（如未保存状态提示/取消回滚），若删除需确认自动保存覆盖所有字段与场景。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_006a.png（顶部工具栏/侧边栏入口）、006b.png（工作动态删除图标）、006c.png（尺寸表删除图标列）、006d.png（右上角取消/保存按钮）；子项③需先核对全站自动保存覆盖范围再决策，删除按钮类需求建议统一封装确认组件；3 个子项可拆分排期。2026-09-07 实施并验证（两轮）：第一轮：①新建 ConfirmModal 统一确认组件（标题/说明/危险确认/取消，遮罩点击取消），全站 11 处删除点统一接入——删除单据（App）、打样批次（SampleRunList）、工作动态条目与设计稿移除（DetailView）、尺寸表单行/批量/清空（SizeTable）、BOM 物料（BomEditor）、工艺指示（ProcessEditor）、号型系列（SizeGroupManager）、测量品类/预设部位（MeasurementTemplateManager）、人员移除（PeopleEditor）、图纸资料/版本记录（DrawingLibrary，上传去重确认因在异步流程内保留原生 confirm）；③首评「保留保存/取消」；E2E 11/11 PASS。第二轮（用户验收修订）：①热区收窄——取消全屏左缘（x≤8）自动展开，改为**仅鼠标靠近顶栏主按钮（logo/菜单，外扩 14px 邻域）自动展开**，杜绝工作区边缘误触；③自动保存改造——核对后端 PATCH 白名单（STYLE_KEYS/TASK_KEYS）已覆盖款式信息全部字段与 progress_nodes，前端实施全字段即改即存：款式信息/打样说明文本 onBlur 提交、下拉即时提交、工作动态条目输入防抖 400ms（添加/删除/状态即时提交）、尺寸表变更防抖 400ms；**移除详情页「保存」「取消」按钮**，整站统一"即改即存"；另按用户验收将测量部位操作列 op-btn 与品类目录 del-btn-mini 统一为 icon-btn 系常驻显示。验证：E2E 9/9 PASS（左缘不误触发/主按钮 hover 展开/顶栏无保存取消/款式名称 blur 落库并还原/工作动态添加删除即时落库/部位操作列统一图标/零 console 错误）+生产构建通过。 |
| REQ-007 | 2026-09-07 | 控件/图标交互样式全局统一：当前各页面操作控件格式不统一——部分操作图标（编辑/删除等）为自感知（悬停才显示或高亮），部分则常驻一直高亮（如系列管理列表行操作、公差/放码规则表操作列、单信息侧栏删除、工作动态条目删除）。需制定并落地统一的控件交互规范：明确操作图标采用「悬停显现」还是「常驻显示」、高亮/选中态样式、图标风格与尺寸，全站各页面（系列管理、公差规则配置、单信息侧栏、工作动态等）按同一规范收敛。 | P2 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_007a.png（系列列表行操作）、007b.png（公差规则表操作列）、007c.png（侧栏删除+工作动态条目删除）；需先定交互规范基线再逐页收敛，建议与 REQ-006 删除确认组件统一设计；涉及面广但单点改动小，适合批量执行。补充案例（2026-09-07）：款式信息页年度/季节/波段下拉框、打样批次页日期输入框等控件样式亦不统一，见 docs/archive/req-screenshots/_req_screenshot_008a.png、008b.png。2026-09-07 与 REQ-006 一并实施：交互基线拍板=**操作图标常驻显示+悬停高亮**（.icon-btn 常规蓝、.icon-btn-danger 危险红，28px 圆角透明底）；批量收敛——系列管理行操作（btn-icon-xs→icon-btn 系）、公差/放码规则操作列（op-btn delete→icon-btn-danger）、工作动态条目删除、尺寸表单行删除、BOM/工艺行删除、图纸资料与版本删除（drawing-del 由悬停显现改常驻）全部统一为 icon-btn 系；控件样式统一——全局 select 去原生箭头自绘深色箭头（option 深色背景）、input[type=date] 深色指示器（color-scheme:dark）。日期完整日历组件见 REQ-009。验证：随 REQ-006 E2E 11/11 PASS+生产构建通过。 |

| REQ-009 | 2026-09-07 | 日历选择器组件视觉重设计：当前日期选择器弹窗样式粗糙（选中日期为黑底白字、月份切换箭头简陋、无整体设计感），与系统深色主题不协调。需按整体 UI 规范重新设计日历组件——统一深色主题下的配色、圆角/间距、选中态与今日标识、月份/年份切换交互、底部「清除」「今天」操作区，并保证全站所有日期输入（任务开始/预计完工/实际完工等）复用同一组件。 | P2 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_008c.png（日历弹窗现状）。实施：新建 DatePicker 组件（src/components/common/DatePicker.jsx）——触发为自定义输入框（日历图标+值显示+占位符），弹窗日历深色主题：周起始周一（一二三四五六日）、月切换左右箭头、今日描边高亮、选中日蓝色填充、非本月灰显补位（点击补位自动切月）、底部「清除」「今天」操作区、点击外部关闭；值与原生 input[type=date] 兼容（yyyy-MM-dd）。全站 5 处日期输入统一替换：批次表单 4 个（面料到库/任务开始/预计完工/实际完工，SampleRunList）+ 工作动态条目日期（DetailView tl-date）。2026-09-07 验证：浏览器 E2E 全 PASS（批次+工作动态日历打开/7 星期表头/35 日期格/今天清除按钮/月份导航/点「今天」落库 2026-09-07/点「清除」还原/全站无原生 date/零 console 错误）+生产构建通过。
| REQ-010 | 2026-09-07 | 系统配色增加自适应主题：当前全站为固定深色配色，需增加自适应配色方案——跟随操作系统设置（浅色/深色）自动切换。实施要点：①将全站配色抽象为 CSS 变量/设计令牌（背景、前景、边框、状态色、图表色等），定义 light/dark 两套取值；②通过 prefers-color-scheme 自动匹配系统方案；③全站收敛（仪表盘、详情页各标签页、批次卡片、弹窗、下拉/日期选择、表格、图表、图片占位背景等）不得残留硬编码颜色；④深浅色下状态标签、图表（品类占比/进度分布）、选中态等需保证对比度与可读性。 | P2 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_010.png（当前深色仪表盘）；方案已拍板（用户 2026-09-07）：**必须加手动切换开关**、深浅配色参考豆包客户端效果。实施：新建 src/styles/theme.css——:root 深色默认 + [data-theme="light"] 浅色（浅灰白底 #f5f6f8/白卡片/深灰文字 #1f2329/#3370ff 品牌蓝，深色沿用原深蓝黑底+亮蓝）；写 _theme_migrate.ps1 批量替换 app.css+24 个 JSX+index.css 的界面色为 var(--xxx)（约 300 处：rgba(255,255,255,x) 界面透明层/深浅文字/边框/16 hex 色，状态色 #ef4444/#4ade80/#fbbf24/#a78bfa 等与遮罩 rgba(0,0,0,x) 两态可读合理保留）；main.jsx 引入 theme.css；App.jsx 主题状态（auto 跟随 prefers-color-scheme + light/dark，写入 document.documentElement.dataset.theme，监听 matchMedia 变化，localStorage 持久化）；设置页三选一「自定义/系统深色/系统浅色」。**用户拍板（2026-09-08 重新实施）**：豆包客户端深浅配色＝系统原生配色（零自定义色差）→ 系统深/浅改用 CSS 系统语义色（Canvas/CanvasText/Field/AccentColor/AccentColorText）+ color-scheme + light-dark() 中性透明层，浏览器自动映射系统真实颜色（深色=#121212 系、浅色=白、强调色=系统 AccentColor）；边界：次要文字/边框/悬停用中性黑白透明层（非自定义色板）、业务状态色保留；浅色下 14 处深色半透明输入框/卡片硬编码背景→CSS 变量（看板筛选框/分类 select/BOM·工艺表格输入框/accent 硬编码等）。验证：E2E 13/13 PASS（三态切换/语义色实际渲染/刷新保持/还原自定义）+看板深浅观感截图目检+设置页深浅目检。**二轮修复（用户实测反馈）**：`.bento-box` 卡片硬编码 `rgba(0,0,0,0.2)!important` 浅色下=中灰 #ccc（截图面积 20%+，惨不忍睹根因）→`var(--card-bg)`/`var(--border-strong)`；全站白半透明边框 23 处+白透明背景 19 处（深色设计模式，浅色下边框消失/背景发灰）→`var(--border)`/`var(--border-weak)`/`var(--bg-panel)`/`var(--bg-hover)` 等；输入框/弹窗内容区黑透明背景 4 处→`var(--input-bg)`/`var(--bg-elev-2)`。复验：浅色看板/详情页全页扫描中灰残留=0、卡片白底清晰边框、观感截图正常。**第②步收敛（配色审计报告 62 处主题色硬编码，2026-09-09 全部收敛）**：按 `docs/archive/配色审计报告.md` 明细逐处收敛——输入框/下拉/日历等背景 `rgba(2,6,23,.5)`×7→`var(--input-bg)`；图纸卡片→`var(--card-bg)`；拖拽区/上传列表/版本行/统计卡/表格容器等中性底→`var(--bg-panel)`；白透明边框 23 处→`var(--border)`/`var(--border-weak)`/`var(--border-strong)`（虚线/强边框→strong）；`#334155`/`#888` 浅色遗留深灰字→`var(--text-3)`；强操作按钮深底（pdf-action/enlarge）→`var(--bg-elev-2)`；蓝灰中性层 `rgba(148,163,184,x)` 背景/边框→`var(--bg-panel)`/`var(--bg-hover)`/`var(--border)`/`var(--border-weak)`；jsx inline 3 处（无批次标签/默认角色标签/历史版本按钮边框）同步收敛。**验证（2026-09-09）**：收敛后全站复查残留硬编码 62→0；浏览器 E2E 11/11 PASS（系统深/浅×看板/仪表盘/设置页 data-theme 属性+中灰残留扫描=0+输入框实际色 dark=rgb(59,59,59)/light=rgb(255,255,255)）+浅色详情页/图纸库/工艺指示/版本弹窗/工作动态事件行/26AWW526 详情/深色详情页截图目检全部正常+零 console 错误+生产构建通过，commit 见本轮。**第③轮（2026-09-09 用户实测反馈：控件自发光过脏+深浅两色主按钮不可见）**：①**主按钮改品牌蓝固定**——新增 `--accent-btn:#38bdf8`/`--accent-btn-text:#06283d`（:root 定义三套主题继承，不随系统 AccentColor），主按钮类（.btn-blue/.btn-blue-sm/.btn-add-mini/.btn-add-circle/.btn-primary）统一改用它——系统 AccentColor 偏淡/偏暗时主按钮不再融背景（豆包主按钮同为品牌色非系统色）；②**弱化全站自发光**——移除 14 处蓝色 box-shadow glow/text-shadow（.btn-glow 常驻 20px/hover 30px、.btn-add-circle 常驻 15px、.btn-blue-sm:hover 15px、.data-table input/textarea:focus 10px、.template-card.selected 15px、.tab.active::after 上发光、.area-dot 10px、.active-mode 10px、.dash-stat-card.active 蓝色 22px 光晕、.bento-style-no 款号文字 10px 发光、.field focus 3px→2px 减淡），保留 inset 描边与 focus 细环；③**设置页款式分类库输入框/下拉边框加强**（.add-row-enhanced input/select border→var(--border-strong)，原 rgba(255,255,255,0.08) 轮廓不可见即用户截图红框"空白"根因）。验证（2026-09-09）：E2E 10/10 PASS（三套主题 +按钮/+新增系列/看板新建打样单背景全 = rgb(56,189,248) 品牌蓝+深字、蓝色自发光扫描残留=0、输入框边框 custom=0.15/dark=0.2/light=0.18）+设置页/看板截图目检正常+生产构建通过，commit 见本轮。**第④轮（2026-09-09 用户实测：控件在系统深/浅下仍不可见——"按钮不见了""输入框空白"，暴怒要求按用户实机视角验证）——根因：用户实机浏览器（豆包内置浏览器）不支持 `light-dark()`/`color-mix()`，系统深/浅主题下所有 light-dark() 变量失效→次要文字/边框/背景全无效→控件隐形**。修复：①**theme.css 系统深/浅块全部展开为显式双值**（不用 light-dark()/color-mix()，任何内核一致渲染）；②**同时加强对比度**——系统深色：text-2 #9aa3b0→#b4bcc8、text-3 #6e7784→#8f99a6、text-4 #4e5663→#67707e、border 0.12→0.18、border-strong 0.2→0.3、bg-panel 0.04→0.06、scroll-thumb 0.3→0.4；系统浅色：text-2 #4e5969→#3d4757、text-3 #86909c→#5c6675、text-4 #a9aeb8→#8b92a0、border 0.1→0.16、border-strong 0.18→0.26、bg-panel 0.03→0.05；accent-soft 改固定蓝 rgba(0,110,255,x)（不再 color-mix）；③**弱化剩余发光**——PdfPickerModal 选中光圈 0.55→0.18、toggle-thumb 黑阴影移除、data-table input/textarea focus 蓝边 rgba(56,189,248,0.4)→var(--border-strong)。**教训（记入开发日志）**：E2E 用系统 Edge（支持 light-dark()）断言"没问题"≠用户实机（豆包内置浏览器不支持）——浏览器特性兼容必须按用户真实环境内核验证，不能仅凭本机浏览器断言。验证（2026-09-09）：E2E 19/19 PASS（三主题设置页/看板蓝色发光残留=0、主题按钮有边框有背景、输入框有边框、看板新建按钮=品牌蓝）+面包屑链接对比度 深8.58/浅9.39、副标题 深5.69/浅5.81 全达 WCAG AA +深/浅设置页、详情页截图目检全部控件清晰可见，commit 见本轮。**第⑤轮（2026-09-09 用户实机复测：Edge 可见但豆包内置浏览器主页按钮仍不可见+背发光仍严重+深浅主题不要亮蓝）**——**根因②：豆包内置浏览器连系统色 AccentColor 也不支持**——logo 图标 `<Layout color="var(--accent)">` 在深浅主题下 --accent=AccentColor 无效→图标变黑→深色下不可见（即用户说的"主页按钮"）；亮蓝 #38bdf8 主按钮/accent-soft 系蓝色半透明选中背景=用户说的"背发光"。**修复**：①**--accent/--accent-btn 加 @supports not (color: AccentColor) fallback**——支持系统色浏览器（Edge）用 AccentColor 零色差，不支持（豆包内置浏览器）回退标准系统蓝 #0a84ff+白字——深浅主题主按钮/强调色跟随系统（去掉亮蓝），自定义主题保留 #38bdf8；CanvasText/Field 同理 @supports fallback；②**蓝色半透明"背发光"全部变量化并在深浅主题弱化**——side-nav-item.active/template-card.selected/cat-item.active/template-item.active/sidebar-add button(含hover 0.3)/dash-bar-row.clickable.active(0.12+inset 0.35)/pdf-enlarge-btn:hover(0.25)/people-suggest-btn:hover/pulse-update 动画等 `rgba(56,189,248,0.1~0.35)` → `var(--accent-soft)`/`var(--accent-soft-2)`（custom 0.14/0.22、dark 0.08/0.16、light 0.06/0.12）——深浅主题选中态变系统蓝淡底；data-table input:focus 蓝底+inset 蓝描边 → var(--bg-hover)+var(--border-strong)；people-role-input 亮蓝边框 #7dd3fc 文字 → var(--accent)+var(--text)。验证（2026-09-09）：E2E 11/11 PASS（三主题 logo 图标可见、主按钮非透明、深浅主题主按钮=系统蓝 rgb(0,117,255)≠亮蓝、亮蓝残留扫描=0、自定义保留品牌蓝）+logo 图标区域像素确认蓝色渲染+深浅看板截图目检干净，commit 见本轮。**第⑥轮（2026-09-09，用户最终拍板并验收方向）：深浅主题零蓝色+零发光，强调色改纯中性灰阶**。前⑤轮误把"不要亮蓝"理解成"换系统蓝 AccentColor"，被用户连续否定——用户真实诉求＝系统深/浅主题里蓝色作为强调色/底色/文字/图标/边框/光晕一律不要（自定义主题保留品牌蓝 #38bdf8 不变）。①theme.css：dark --accent/--accent-btn 改 #e6e8eb（近白）、--accent-btn-text #1a1a1a（浅底深字）、--accent-soft/soft-2 改 rgba(255,255,255,.1/.18)；light 改 #1f2329（近黑）+白字、soft rgba(0,0,0,.06/.12)；删除 @supports AccentColor 回退块（深浅不再跟随系统强调色，故无需蓝回退，CanvasText/Field 回退保留）。②全站蓝色硬编码收敛到变量（深浅自动中性、自定义仍蓝）：app.css 38 处、index.css 6 处 rgba(56,189,248,x) 背景→var(--accent-soft)、边框→var(--accent-soft-2)、强态→var(--accent)/var(--accent-btn)；#7dd3fc→var(--accent)；JSX 内联 ExportButton/PdfPickerModal/ProcessEditor/KanbanView 表头分隔线/PeopleEditor 设计师角色色/DetailView 拖拽态与提示字/DrawingLibrary emf 类型色（#0ea5e9→中性灰 #94a3b8）同步收敛。③零发光：新增 --app-bg 变量（自定义保留蓝/红径向氛围光，深浅=var(--bg) 纯色，移除页面光晕）；.glass 阴影错用 var(--input-bg)（浅色下=白→白阴影发光）改 var(--shadow)；.tab-bar 半透明+blur 改 var(--bg-elev) 实色去模糊；深浅主题 .glass/.glass-card 统一 backdrop-filter:none+var(--card-bg) 实色（毛玻璃透出的模糊光斑即"脏/发光"来源）。验证（2026-09-09，真实 Edge 渲染逐页目检，非仅数据断言）：三主题看板/详情尺寸指标/图纸资料/设置全页截图——深色 logo 白、Tab/标准码表头/版次锁定行/规则标签全中性、主按钮浅底深字；浅色同元素近黑、主按钮黑底白字；自定义品牌蓝（logo/Tab/主按钮/标准码/版次行蓝底）完整保留；vite build 通过。 |
| REQ-011 | 2026-09-07 | 历史版本功能（改错可追溯可回滚）：当前详情页已实现全字段「即改即存」自动保存，改错后无从追溯。需增加历史版本能力：每次自动保存为款单生成版本快照，可按时间回溯查看各版本内容、对比版本间差异（哪些字段在何时被改成了什么），并支持一键回滚到历史版本。 | P1 | 已完成 | 提出背景：2026-09-07 用户验收 REQ-006③ 自动保存改造时提出（"错了都不知道哪错了"）；与既有「操作日志」（关键动作记录）互补——历史版本面向内容快照与回滚，操作日志面向动作记录。方案已拍板（2026-09-07 用户）：快照范围=全量（款式字段+任务字段+工作动态+尺寸表+BOM+批次，批次仅展示不参与回滚）；版本粒度=5 分钟编辑会话合并（同款多次自动保存合并为一条版本，避免版本爆炸）；对比=版本列表+字段级旧→新对比+尺寸表/BOM 按行增删改对比；回滚范围=款式+尺寸表+BOM（批次为独立工作流不回滚），回滚本身生成新版本可再次撤销；入口=详情页顶栏「历史版本」按钮。实施：迁移 v15 新建 task_versions 表（id/task_id/version_no/snapshot JSON/summary/created_at）；server/services/versions.cjs（buildSnapshot 全量快照/5 分钟窗口合并 capture/版本列表 list/diffSummary 变更摘要/rollback 事务回滚+生成新版本）；tasks.cjs update() 末尾挂 capture（PATCH 落库即记版本，款式与任务字段共用入口）；routes/versions.cjs 三接口（GET 列表/GET 单版详情/POST 回滚）；前端 VersionHistoryModal 组件（列表→详情对比→回滚二次确认），详情页顶栏「历史版本」按钮接入，回滚后强制刷新详情+BomEditor 重拉。验证：HTTP 全链路（PATCH→版本生成/5 分钟合并/回滚/回滚留痕）+浏览器 E2E 全 PASS（弹窗列表 4 条/详情对比/回滚按钮/零 console 错误）+生产构建通过。注：修复过 created_at 时区不一致（表默认 UTC 导致 5 分钟合并判断错乱，显式 datetime('now','localtime')）。 |

| REQ-008 | 2026-09-07 | 系统设置页现「设计师预设」控件升级为「人员预设」控件：可增加不同人员（姓名）并为每人配置角色（如纸样师/样衣工/设计师等，一人可多角色，角色可自定义添加）；系统各处人员选择（款式设计师、批次版师/样衣工）联动从人员预设按角色过滤。 | P1 | 已完成 | 实施：新增 PeopleEditor 组件（人员=姓名+角色集，角色快捷添加+自定义），替换设置页「设计师库」卡；settings 新增 people=[{name,roles}]，后端读取层兼容迁移旧 designers（自动转角色=设计师，不写回库）；联动：详情页设计师 SmartSelect、看板「全部分派设计师」筛选、批次版师/样衣工 SmartSelect 均按 peopleByRole 角色过滤（保留自由输入）。2026-09-07 验证：HTTP 迁移确认（白洁/李飞→设计师角色）+浏览器 E2E 12/12 PASS（迁移展示/加人/加角色/批次版师下拉联动含新人员且不含他角色人员/删除恢复/零 console 错误）+生产构建通过。 |
| REQ-012 | 2026-09-07 | 统一导航栏并评估返回箭头去留：当前各页面顶部导航栏样式不统一——仪表盘页左侧有返回箭头（←）但其本身为顶级页面、无上一级，右侧为「菜单」「新建打样单」；列表页为「PatternMaster Pro」logo+名称+「仪表盘」「新建打样单」；详情页为返回箭头+面包屑（主页/打样需求单/编辑）+操作按钮（导出工艺单/导出PDF/历史版本/删除单据）。期望：①制定统一的导航栏规范（左区：返回/面包屑/标题；右区：操作区）并全站收敛；②评估返回箭头去留：仪表盘等顶级页面返回箭头无上一级应移除；详情页返回箭头与面包屑功能重复，建议统一为面包屑（可点击回退）或统一保留箭头，避免并存。 | P2 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_011a.png（仪表盘导航）、011b.png（列表页导航）、011c.png（详情页导航+面包屑）。规范拍板：顶级页（看板/仪表盘/设置）无返回箭头、保留品牌/标题区；详情页移除←、面包屑前两级（主页/打样需求单）可点击回退、当前级（编辑）灰色不可点；右区统一操作按钮；侧边栏热区全站统一为仅图标（sidebar-hotzone 包图标，仪表盘菜单按钮同步从「整按钮含文字」收敛为图标热区，与 REQ-006 修订3 一致）。2026-09-07 验证：浏览器 E2E 8 项全 PASS（看板无箭头/进详情/详情无箭头/面包屑两级链接可点/当前级=编辑/点击面包屑回看板/仪表盘无箭头/菜单图标热区结构）+生产构建通过。2026-09-07 修订（用户验收）：①仪表盘顶栏菜单按钮从右侧移到左侧 logo 区（与其他页面 logo 结构一致，仅图标热区），右侧仅留操作按钮；②主页顶栏「仪表盘」按钮取消（侧边栏可便捷切换）；③设置页「返回主页」按钮取消（同上）；④修复仪表盘无法滚动——根因 .app 容器 overflow:hidden + .dashboard-view 仅 min-height:100vh，内容超高后无滚动容器，改 height:100vh+overflowY:auto+custom-scrollbar（与设置页一致）。验证：浏览器 E2E 7/7 PASS（主页无仪表盘按钮/仪表盘菜单按钮在左侧 logo 区/右侧仅新建打样单/设置页无返回主页）+小视口滚动实测（500px 下 scrollTop 生效）+生产构建通过，commit 见 feature/sample-run-model。 |
| REQ-013 | 2026-09-07 | 修复系统设置页「新增系列」「新增部位」对话框打不开：在系统设置页点击「号型规格系列设定」区的「+新增系列」按钮，以及「尺寸部位管理」区的「+新增部位」按钮，对话框均不弹出，功能不可用；款式分类库右侧「+」、品类目录「+」疑似同病。期望点击后正常弹出对应新增表单，行为与编辑弹窗一致。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_013.png（红框标注两处入口）；用户怀疑路由丢失——设置页内新增交互未绑定/组件未挂载/路由缺失，需开发会话排查根因；疑似涉及 SizeGroupManager（新增系列）与 MeasurementTemplateManager（新增部位）；复现环境：浏览器 http://localhost:5173 系统设置页，截图取自 2026-09-07。2026-09-08 复现验证（与 REQ-016 同轮）：**不复现，功能正常**——设置页「+新增系列」点击弹出新增表单、「新增部位」（MeasurementTemplateManager，按钮文本无 + 号）滚动定位后点击弹出新增表单，均正常渲染。 |
| REQ-014 | 2026-09-07 | 工艺指示「工艺要求/做法」输入空间不足：详情页工艺指示标签页表格中「工艺要求/做法」列宽固定且过窄，长文本被截断显示不全（如第 1 项「投产前逐一核对样衣、纸样、工艺制单，…」），无法承载完整工艺要求描述。期望：①输入框/列宽可调整大小，最好根据内容自适应——文本越长列宽/行高自动扩展（或输入时自动撑开）；②开放手动拖拽调整列宽的方式。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_014.png（红框标注「工艺要求/做法」列）；涉及组件 ProcessEditor（工艺指示表格，共 13 项）；「标准/参数」「备注」等长文本列存在相同问题，开发时一并评估；需确认当前表格是否有横向滚动/自适应机制；优先级默认 P1（影响录入可用性），可调整。实施（2026-09-08 12:59，commit 45c1dae）：①长文本列（工艺要求/做法、标准/参数、备注）改**自动撑高 textarea**——行随内容增长、超 5 行内部滚动，不再截断；②表头**拖拽调列宽**——ColDragHandle（mousedown/mousemove 实时调整、表头分隔线悬停 cursor:col-resize），列宽 localStorage 记忆（proc_col_widths_v1）；app.css 新增 .col-drag 手柄样式。此前已完成但未同步文档，本行补记。 |
| REQ-015 | 2026-09-07 | 版师改为款级字段：当前「版师」为批次（打样单）级字段——详情页基本信息标签页批次配置区独立显示版师（如 MR.Y，见截图红框），同一款式的多个批次各自维护版师，与实际业务不符（一款由同一版师负责全程版型开发）。期望：版师上移至款式层维度，与款号/名称/类别/设计师等款级共享字段并列（样式信息区块），款下所有批次共享同一版师，批次配置区不再独立显示版师；款式清单「版师」列改取款级字段。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_015.png（红框标注批次配置区「版师」）；与 REQ-003（版师/样衣工下沉批次 v13）、REQ-004（款/版次归属重构）相关，本需求为对 REQ-003 版师归属的修正拍板；**样衣工归属已拍板（2026-09-08 用户）：维持版次级不上移**——理由"同一款的多版次样衣不一定是同一个人做的"，批次配置区仅移除版师字段、保留样衣工；款式信息区块编辑入口同步补充版师字段。实施（2026-09-08）：迁移策略已拍板（用户）＝**取该款「最近的非空版师批次」上移**（不丢已填数据）。迁移 v17：styles 加 pattern_maker 列 + 按同款 sample_runs 取 sort_order 最大且 pattern_maker 非空回填；后端 tasks.cjs：查询带出 s.pattern_maker、STYLE_KEYS 白名单加 pattern_maker、create 建款写入；前端：①详情页款式信息区块新增款级「版师」字段（SmartSelect，人员预设按版师角色过滤）；②批次配置区（SampleRunList）移除版师字段、保留样衣工；③设计师仪表盘「版师」列改取款级 t.pattern_maker（样衣工仍取最先进批次）；④看板批次行负责人只显示样衣工（版师款级化后不再逐批次重复）。验证：迁移回填（26AWW526/235-6→MR.Y、其余空）+HTTP PATCH 读写+浏览器 E2E 9 项（详情版师字段/批次区无版师有样衣工/编辑 PATCH 发出/reload 保持/看板批次行无版师/仪表盘版师列=MR.Y）+测试后数据恢复原值，commit 见本轮。 |
| REQ-016 | 2026-09-07 | 修复全站系统级按钮失效：几乎所有系统级操作按钮点击无响应，已复现范围——列表页「+新建打样单」「导出」、仪表盘「+新建打样单」、详情页「导出工艺单/导出PDF/历史版本/删除单据」、设置页「+新增系列」「+新增部位」等，均无法触发对应功能。期望全站按钮恢复正常点击响应，并排查是否存在未覆盖的同类失效。 | P0 | 已完成 | **【2026-09-08 最终结论：确认复现并已修复，用户意见正确】**根因＝提交 76f12ec（REQ-006 修订3）将 `.overlay` 默认样式改为 `opacity:0; pointer-events:none`（为侧边栏淡入动画），但仅 App.jsx 侧边栏同步加 `.overlay-show`，其余 8 个复用 `.overlay` 的弹窗组件（NewTaskModal/ExportButton/OperationLogsModal/PdfPickerModal/PdfThumb/DrawingLibrary×2/SizeGroupManager/MeasurementTemplateManager）未同步 → 全站弹窗**透明不可点**（DOM 在但 opacity:0、pointer-events:none，elementFromPoint 命中下层看板卡片），即用户所见"按钮全部无效"。此前 9/8 上午"不复现"结论系**测试缺陷**：E2E 仅断言 overlay DOM 存在，未校验 opacity/pointer-events/elementFromPoint。修复：8 组件 `className="overlay"` → `"overlay overlay-show"`（共 9 处，git 提交见本轮）。验证（真实点击+原生 setter 填表）：新建打样单完整流程——弹窗可见（opacity1/pe auto/命中按钮）→填表→「确认创建」→POST /api/tasks→新单出现看板；导出确认框、设置页「新增系列」「新增部位」弹窗均可见可点；测试单已清理。服务稳定性：改由计划任务 **PMProDev** 一次性启动（`scripts/start-dev-task.bat`，纯启动非守护，任务计划程序托管，不依附 agent 会话），观察持续稳定性；启动命令 `Start-ScheduledTask -TaskName PMProDev`。**服务反复挂最终根因（同日取证确认）**：Vite 文件监听（FSWatcher）因 EBUSY 崩溃——`dev_err.log` 铁证 `Error: EBUSY: resource busy or locked, watch _chrome_fix3\Default\Sessions\...`；触发条件＝测试把 Edge 用户数据目录（_chrome_*）放在项目根目录，Edge 运行中锁定 Sessions 文件，Vite 默认监听全目录（vite.config.js 无 server.watch.ignored）→ EBUSY → 未捕获 error → Vite 进程崩 → 服务挂；三次挂机时间全部对应测试时段，与启动方式/目录迁移无关。修复：vite.config.js 加 server.watch.ignored（忽略 **/_*/**、**/_*.*、attachments_download、server/backup_empty）；并规范 E2E userDataDir 放项目目录外（%TEMP%）。 二度触发（同日晚）：编辑工具写文件生成的 *.agent_infra_tmp* 临时文件（如 docs/待开发文档.md.agent_infra_tmp_*）同样 EBUSY 崩溃，已追加忽略 `**/*.agent_infra_tmp*` 并实测生效。验证：项目内模拟测试目录+Edge 实例运行（Sessions 锁定）→ 30 秒后服务存活（修复前必崩）。**测试方法论教训（用户强调，必须记下）**：测试前端问题不能仅凭数据说话——E2E 断言 overlay DOM 存在不等于弹窗可见可点（曾漏检 opacity:0+pointer-events:none 导致误判"不复现"）；用户反复反馈无效时必须按用户视角深挖（可见性/可点击性/真实点击命中），并以真实浏览器操作验证。 |
| REQ-017 | 2026-09-08 | 修复尺寸表「录入成衣实测」对比功能 3 处缺陷：①进入「录入成衣实测」工作模式即整体标红——不应默认标红，应仅在偏差超公差时对对应单元格标红；②成衣实测与标准码存在偏差但「报警/偏差」列计算未生效——已录入数据（如标准码 M=55 vs 实测 52、120 vs 115）时该列仍显示 0.0，应实时计算并显示偏差值（如 -3/+3），超出公差范围时触发报警样式；③「录入成衣实测」按钮未激活前的命名有歧义，需改为语义明确、无歧义的命名（如「进入实测录入」「录入模式」等，具体文案开发时与用户确认）。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_017a.png（尺寸指标页，红框标注按钮与标准码/成衣实测/报警偏差列）、017b.png（尺寸表操作区含「指令/拓码维护」）；与 REQ-005 子项②「标准/实测比对」相关——REQ-005 整体方案点未拍板，本条为既有功能的独立缺陷修复，开发时注意与 REQ-005 联动避免重复实现；涉及 SizeTable 组件偏差计算与标红逻辑、按钮文案。实施（2026-09-08）：**②根因**＝公差解析用 `replace(/[±\+\-]/g,'')` 只去符号，遇预设历史公差格式 `"(±) 0.5"`（带括号）→ parseFloat 得 NaN → checkOutLimit 走 isNaN 分支返回 diff:0 → 偏差列恒显 0.0 且永不标红；修复＝tolV 改 `match(/\d+(\.\d+)?/)` 取首个数字（兼容 "±0.5"/"(±) 0.5"/"0.5"）。**①**：当前代码已符合（实测空值不标红，仅偏差超公差对对应单元格标红），浏览器实测确认无值不标红。**③按钮文案**：按用户验收定稿「**成衣尺寸核对**」（激活/未激活同文案，激活态品牌蓝高亮非红，title 区分进入/退出）。**用户验收修订（2026-09-08）**：字段名（表头「成衣实测」「报警/偏差」）、成衣实测输入框数据、按钮本身**均不标红**（原实现表头/输入框粉红 #fb7185、按钮激活红已全部移除）；**仅超公差的偏差数值标红**（+红色报警图标 AlertCircle）。验证：浏览器 E2E 7 项 PASS（按钮文案×2/激活态蓝色非红/表头不标红/输入框不标红/超差 -3.0 数值标红/报警图标渲染）+测试实测值已恢复清空，commit 见本轮。 |
| REQ-018 | 2026-09-09 | 版次（打样单）数据模型补全，完整覆盖一件样衣制作全流程。按用户拍板的字段分组：①版次基本属性——单号、版次、尺码、颜色、打样件数（默认 1 件）、审核状态、审版意见、备注；②进度——当前状态、优先级、阻塞原因；③纸样相关属性——版师、纸样完成时间；④样衣相关属性——样衣工、样衣完成时间、预期完成时间、面料到库时间、辅料到库时间；⑤资料——尺寸表、面辅料清单、纸样。期望版次卡片表单按上述全量字段组织展示与录入，字段不缺项。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_018.png（当前批次配置区）；现状对照：①②④中除「样衣完成时间/辅料到库时间」外均已有（单号 PO-xxx-V{n}/版次/尺码/颜色/打样件数默认1/优先级/阻塞原因/状态/样衣工/审核状态/审版意见/备注/预计完工/面料到库/实际完工/尺寸表/物料清单）；**需新增字段：纸样完成时间、辅料到库时间**；待明确：样衣完成时间与既有「实际完工」是合并还是拆分；③纸样相关「版师」与 REQ-015（版师改款级字段）存在表述差异——本次清单把版师列入版次「纸样相关属性」，需确认是款级版师继承展示在版次卡上、还是改回批次级可编辑；与 REQ-004（款/版次归属）、REQ-015（版师上移）、REQ-005（尺寸表归属版次）联动实施，预计涉及 sample_runs 加列与表单分组重排。**决策（2026-09-09 用户拍板）**：样衣完成时间与「实际完工」本来就是同一字段（finish_date），不加新列；版师同理不需要增加（保持款级 REQ-015 决策，不进批次卡）；表单保持单卡片长表单，内部按 5 组小标题分区（不折叠）。**实施（2026-09-09）**：迁移 v18——sample_runs 加 pattern_date（纸样完成时间）、accessory_date（辅料到库时间）；后端 sampleRuns.cjs FIELDS 白名单同步；SampleRunList.jsx 批次卡片按 ①版次基本属性（单号/版次在卡片头+尺码/颜色/件数/审核+审版意见/备注）②进度（优先级/阻塞原因/任务开始，当前状态在卡片头）③纸样相关（纸样完成时间）④样衣相关（样衣工/实际完工/预期完成/面料到库/辅料到库）⑤资料（尺寸表/物料清单/纸样三个入口，跳转详情 Tab）五组重排；DetailView 传 onOpenBom/onOpenDrawings；新增 .run-section-title/.run-materials-row/.run-material-btn 样式。**验证**：全新库 v1-v18 迁移完整性 4 项 PASS（v17 版师列与新 v18 列共存）；HTTP 新字段读写链路 8 项 PASS（PATCH 读写/局部更新不串字段/恢复空值/版本接口兼容）；浏览器 E2E 28 项 PASS（5 组分区标题/REQ-018 全量字段不缺项/资料三入口/新字段录入落库 reload 保持/资料入口跳转/零新增 console 错误）；生产构建通过（8.7s）；测试数据已恢复（10 批次新字段全空，26AWW526 V0/V1/V2 未触碰），commit 见本轮。 |
| REQ-019 | 2026-09-09 | 修复「人员预设」中已存在人员无法重新编辑的问题：设置页「人员预设」（PeopleEditor）中已添加人员的姓名目前为纯文本展示，无任何编辑入口——姓名录入错误（错别字/大小写/多余空格）或需改名时只能先删除再重新添加，且删除仅移除预设、历史单据中引用旧姓名的记录不受影响，换名即断关联。期望：已存在人员的姓名支持重新编辑（点击姓名进入编辑态/编辑按钮，保存后即时落库），编辑交互与整站「即改即存」一致，重名校验与新增时一致；同时明确编辑姓名后与历史引用（款式设计师/批次版师/样衣工/看板筛选等已按旧姓名落库的记录）的关系，是否同步更新或仅影响后续引用。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_019.png（红框为人员列表，姓名均不可编辑，仅有角色快捷添加 + 与删除 ×）；涉及组件 src/components/settings/PeopleEditor.jsx（REQ-008 新建）；现状核对（2026-09-09 读码确认）：people-row 内姓名为 `<span class="people-name">` 纯文本，角色支持 + 添加 / 标签 × 删除、人员支持 × 删除（ConfirmModal 确认），唯独姓名无编辑入口；人员数据 [{name, roles}]，全站人员选择（款式设计师 SmartSelect / 批次版师·样衣工 / 看板「全部分派设计师」筛选）按 name 引用联动；与 REQ-008（人员预设控件）同组件，删除确认沿用 REQ-006② ConfirmModal。**决策（2026-09-09 用户拍板）**：改名后**同步更新全站引用**（styles.designer / 批次版师·样衣工）；历史版本快照与操作日志不回改（保持历史记录语义）。**实施（2026-09-09）**：①后端新增 `PATCH /api/people/rename`（settings.cjs renamePerson 事务：settings.people + 遗留 designers 兼容数组 + styles.designer + sample_runs.pattern_maker/sample_maker 同步；重名/不存在/空名拒绝、同名幂等；IPC `settings:renamePerson` 同步注册）；②前端 PeopleEditor 姓名改为可点击编辑（Enter/blur 即改即存、Escape 取消、重名校验与新增一致、列表 key 改稳定索引），改名成功后 loadSettings+loadTasks 刷新全站显示；③新增控件样式 .people-name-input/.people-name-pen（hover 提示）。**验证**：HTTP 全链路 27 项 PASS（预设/designers/styles/runs 三处引用同步+重名/不存在/空名拒绝+事务恢复）；浏览器 E2E 17 项 PASS（点击进入编辑态/Enter 保存/页面即时更新/reload 保持/重名阻止编辑态保持/Escape 取消/blur 即改即存/角色·删除功能回归/零新增 console 错误）；生产构建通过（9.0s）；测试数据已恢复原状（白洁/SS26-DR002 designer/MR.Y 与基线一致），commit 见本轮。**核查补丁（2026-09-09）**：全面核查发现 renamePerson 同步范围缺 `styles.pattern_maker`（REQ-015 版师上移的款级字段，style 5/6 = MR.Y 有真实数据、详情页款式信息区块/清单「版师」列/看板多处展示）——原实现只同步 styles.designer + sample_runs.pattern_maker/sample_maker，版师改名后款级字段残留旧名。已修复：renamePerson 事务第 3 步补 `UPDATE styles SET pattern_maker=? WHERE pattern_maker=?`，返回 styles 计数并入该行。验证：HTTP 改名全链路 16 项 PASS（MR.Y→MR.Y_TMP：styles 2 + runs 4 全同步、无残留；改回完全恢复基线；重名/不存在拒绝；designers 兼容数组不受非设计师改名影响）。 |
| REQ-020 | 2026-09-09 | 统一当前页面同类控件的尺寸与格式：当前详情页（打样需求单编辑页）控件大小、格式混乱，需统一所有同类控件格式。现状核对（2026-09-09 读码确认，SampleRunList.jsx）：同一张批次表单内「下拉选择」混用两种形态——版次/样衣工用自定义 SmartSelect（聚焦描边色与原生控件不同、无统一类），状态/尺码/优先级/阻塞原因/审核状态用原生 select（自绘深色箭头但无统一类）；版次 SmartSelect 外包固定宽 150px、样衣工 SmartSelect 不固定，同控件宽度策略不一致；日期用 DatePicker（占位「年/月/日」）与文本 input（占位「如:黑色」）、select（占位「选择尺码」）占位样式各异；文本/数字 input、textarea（审版意见带内联 style）高度、边框、圆角、聚焦态风格不一；状态/审核/阻塞上色混用内联 style；「尺寸表」按钮 btn-ghost-sm + 大量内联样式（border/padding/borderRadius/fontSize）与全局 icon-btn 系/btn 系不一致。期望：制定并落地表单控件统一规范——同类控件统一高度/宽度策略/边框/圆角/聚焦态/占位样式/颜色应用方式（值色走 CSS 变量而非内联 style），至少当前详情页批次表单全量收敛，并评估同型控件（select/SmartSelect/DatePicker/input/textarea/按钮）全站一致性；规范需与既有基线一致（REQ-007 交互基线、REQ-010 主题变量、REQ-009 DatePicker）。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_020a.png（详情页款式信息+打样批次全貌）、020b.png（样衣工 SmartSelect 展开态）、020c.png（尺码/打版状态/阻塞原因下拉展开态）、020d.png（2026-09-11 视觉裁决：绿框=标准外观/红框=不接受）；涉及组件 src/components/task/SampleRunList.jsx、src/components/common/SmartSelect.jsx、src/components/common/DatePicker.jsx 及全局表单样式；与 REQ-007（控件交互基线统一）、REQ-008（SmartSelect 人员联动）、REQ-009（DatePicker 日历组件）、REQ-010（主题 CSS 变量）联动，开发时先出控件规范基线（高度/宽度策略/边框/圆角/聚焦/占位/上色方式）再批量收敛；建议优先级可上调至 P1（当前为 P2，与 REQ-007 同类定位）。**实施（2026-09-09）**：落地表单控件统一基线——高度 min-height 36px、圆角 8px、字体 13px、占位 var(--text-4)、聚焦 accent ring、宽度 .field 内 100%；app.css `.field input/select/textarea` 统一收敛（原 11px 14px·10px·14px 已对齐），删除 `.run-grid` 独立覆盖（12.5px/6px 8px），补 `::placeholder` 统一占位色，SmartSelect/DatePicker 打开态补聚焦 ring（`.ss-display.open`/`.dp-input.open`）；版次 SmartSelect 去除外包固定 150px div，改 `.run-card-title .smart-select { min-width: 140px }` 统一宽度策略；状态/审核/阻塞值色改 CSS 变量 `--sel-color` + 语义类（`.run-status-select`/`.audit-select`/`.blocker-select`），去除内联色值；审版意见 textarea 去内联 style，统一走 `.field textarea`（110px/resize/line-height 1.6）；「尺寸表」按钮已在 REQ-018 统一为 .run-material-btn。全站同型控件（.field 系/SmartSelect/DatePicker 为共用类）详情页各 Tab、新建打样单弹窗同步受益。**验证**：浏览器 E2E 15 项 PASS（select/input/DatePicker/SmartSelect 高度差 ≤1.5px、圆角统一 8px、字体统一 13px；版次下拉自适应 140px 无固定外包；DatePicker/SmartSelect 打开态聚焦 ring；值色经 --sel-color 生效；textarea 无内联样式；input::placeholder 与 DatePicker 占位色一致；零新增 console 错误）+ 视觉截图确认布局规整 + 生产构建通过（8.7s）；无数据变更，commit 见本轮。**2026-09-11 用户验收驳回（本条重开：已完成→待开发）**：2026-09-09 实施只统一了高度/圆角/字体，下拉外观仍未统一——用户以 _req_screenshot_020d.png 视觉裁决：✅ **绿框＝标准外观**（版次 SmartSelect：深色圆角填充触发框 + 下拉面板带「手动输入自定义值…」输入行与「预设选项」分区）；❌ **红框＝不接受**（打版状态「打版中」、审核状态「未提交」、阻塞原因「无阻塞」三处原生 select 及其黑色原生下拉面板）。重开后实施口径：所有下拉类控件（打版状态/尺码/优先级/审核状态/阻塞原因等原生 select）统一改为绿框 SmartSelect 外观（触发框与下拉面板与版次/样衣工一致），不得再出现黑色原生下拉面板；固定枚举字段是否保留「手动输入自定义值」自由输入行开发时与用户确认，触发框与面板外观必须与绿框一致。截图中蓝色框为浏览器选区误框，非标注意图。**重开实施（2026-09-11）**：①SmartSelect 组件增强——支持 `{key,label}` 枚举选项（显示 label、提交 key）、`allowCustom` prop（固定枚举传 false 隐藏「手动输入自定义值」行，版次/样衣工等可自定义字段保留）、`className`/`style` 透传（`--sel-color` 值色注入根节点由子级继承）；②SampleRunList 五处原生 select（打版状态/尺码/优先级/审核状态/阻塞原因）全部替换为 SmartSelect（枚举 allowCustom=false 保持语义、值色走 --sel-color：状态按状态色/审核按审核色/阻塞非 none 红）；③app.css 清理废弃 `.run-status-select`/`.audit-select`/`.blocker-select` 类，`.ss-display` 支持 `--sel-color`，卡片头状态 SmartSelect min-width 96px（版次 140px 不变），面板沿用既有深色绿框样式（bg-elev-2/border-strong/12px 圆角/选中 accent-soft 高亮）。**验证（2026-09-11）**：浏览器 E2E 25 项 PASS（批次表单 0 原生 select、7 个 SmartSelect 触发框高度/圆角/字体与版次一致、状态 5 项/审核 4 项/阻塞 5 项选项正确且面板无自定义输入行、版次面板保留自定义输入行、当前值高亮、值色经 --sel-color 生效、改状态落库并恢复、零 console 错误）+ 视觉截图目检（触发框统一深色圆角、状态面板深色预设选项分区、无黑色原生面板）+ 生产构建通过（9.9s）；测试数据无残留（状态已恢复样衣中），commit 见本轮。 |
| REQ-021 | 2026-09-11 | 版次卡片内「审版意见」与「批次备注」下移至模块底部：当前二者位于「版次基本属性」组（尺码/颜色/件数/审核状态之后，见截图红框），挤占顶部主属性区；期望将「审版意见（各版次独立）」多行文本与「批次备注」单行输入整体下移到版次卡片最底部——全部字段组（版次基本属性/进度/纸样相关/样衣相关）之后、资料入口（尺寸表/物料清单/纸样）附近，作为版次模块收尾字段；不改动字段本身、绑定与自动保存逻辑，仅调整二者在卡片内的位置顺序。 | P2 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_021.png（红框=当前审版意见+批次备注位置，位于「版次基本属性」组下方；卡片现状为 REQ-018 五组分区：版次基本属性/进度/纸样相关/样衣相关/资料）；涉及组件 src/components/task/SampleRunList.jsx；落点细节：下移后位于「样衣相关」组之后、「资料」按钮行之前或之后，开发时按视觉自然感确定；与 REQ-018（五组分区重排）、REQ-020（控件统一基线）联动，仅位置调整无数据变更。**实施（2026-09-11）**：SampleRunList 中「审版意见（各版次独立）」textarea 与「批次备注」input 整体移出「版次基本属性」组，下移至「资料」按钮行（尺寸表/物料清单/纸样）之后、「绑定资料版本」区之前，作为卡片收尾字段；字段绑定/自动保存逻辑未动。**验证（2026-09-11）**：随 REQ-020 重开 E2E 25 项中顺序断言 PASS（DOM 位置：资料按钮 < 审版意见 < 批次备注 < 绑定资料版本；审版意见不再位于版次基本属性组）+ 视觉截图目检（意见/备注位于资料入口下方、绑定区上方）+ 生产构建通过；无数据变更，commit 见本轮。 |
| REQ-022 | 2026-09-11 | 修复进度计算逻辑与实际数据对不上：看板「逾期情况」分组下，逾期/进度展示与底层数据严重不符。截图实证（docs/archive/req-screenshots/_req_screenshot_022.png，当前 2026-09-11）：①SS26-DR002（白洁，头版样/样衣中·审核已通过）显示「逾期 13 天」，但其底部节点配料日期为 2026-03-10（距今约半年）；②SS26-TS003（YI YU，胚样/待配料+生产样/打版中·审核未提交）显示「逾期 185 天」，配料节点仅 03/01——进度更快的卡反而只逾期 13 天，两卡逾期天数与可见日期数据对不上；③底部阶段节点（配料/跟版/版师/样衣/工艺）中版师/样衣/工艺全为「--」，与实际批次数据（款级版师、批次样衣工等）不符。期望：梳理并修复进度计算权威口径——逾期天数基于合理交期基准（明确款级 expected_date 与 REQ-018 批次级 expected/finish 日期的关系，旧单款级字段不更新不得导致天数失真）；底部进度节点反映版次化后的真实进度（最先进批次状态/实际负责人/实际日期），不得出现「数据已推到 3 月、卡片却只逾期 13 天」类矛盾；看板分组、逾期角标、进度节点、版次条四处口径必须一致。 | P0 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_022.png（看板「已逾期」列：红框=SS26-DR002 标题+「逾期13天」角标；第二张卡「逾期185天」）；现状核对（2026-09-11 读码 KanbanView.jsx）：①逾期 getOverdueInfo 仅读 task.expected_date（款级单字段 YYYY-MM-DD），未纳入 REQ-018 批次级 expected_date/finish_date，旧单款级 expected_date 长期不更新即天数失真；②底部节点渲染 task.progress_nodes（工作动态聚合），REQ-015 版师上移/REQ-018 批次化后该聚合未同步更新（故版师/样衣全 --）；③版次条渲染 taskRuns(t) 的 sample_type+RUN_STATUS_META；后端聚合（progress_nodes 生成/款级状态同步）在 tasks.cjs/DetailView；与 REQ-003③（进度突出最先进批次）、REQ-015（版师款级化）、REQ-018（批次级日期补全）联动；开发时先拍板「进度」权威口径（进度以最先进批次状态为准？交期以款级还是最紧批次日期为准？）再修，避免头痛医头。**口径拍板（2026-09-11，依据既有决策链推导）**：①进度权威=最先进批次（延续 REQ-003③；后端 deriveStyleStatus/findTopRun 同口径）；②交期基准=最先进批次预期完成时间（批次表单可维护、闭环）→ 该款全部批次中最早的预期 → 款级 expected_date 仅旧数据兜底 → 无则不算逾期；③完成判定=款级完结或最先进批次已完成；④底部进度节点=最先进批次真实数据派生（配料=面料到库/跟版=任务开始/版师=纸样完成+款级版师/样衣=实际完工+批次样衣工/工艺无对应字段），不再读手填工作动态（工作动态仍在详情页完整可编辑）。**实施（2026-09-11）**：①后端 tasks.cjs attachRuns 投影 `top_run`（最先进批次摘要：expected_date/fabric_date/start_date/pattern_date/finish_date/sample_maker/audit_status/status/sample_type，与 derived_status 同源 findTopRun），保证前后端口径一致；②前端 KanbanView.jsx getOverdueInfo 重写（基准三级回退+完成判定，返回 due 供角标 title）、逾期角标 title 标注「最先进批次」、底部节点改 buildRunNodes 固定 5 节点批次派生（含负责人 title）、看板分组列自动随新口径（overdue/today/soon/ok/none）。**验证（2026-09-11）**：HTTP 确认 top_run 投影（DR002=头版样 sample_making 预期 09-11；TS003=生产样 pattern_making 预期 03-25）；浏览器 E2E 13 项 PASS（DR002 角标「今日到期」不再 13 天、TS003「逾期 170 天」不再 185 天、节点=批次真实数据配料 03-01/跟版 03-08/样衣 08-27、TS003 生产样无日期节点 --、节点固定 5 个、版次条无回归、DR002 归「今日到期」列/TS003 归「已逾期」列、零 console 错误）+ 视觉截图目检（逾期分组三列、TS003 逾期 170 天、DR002 今日到期、AW26-PA004 今日到期、节点与版次条口径一致）+ 生产构建通过（9.9s）；测试无数据写入，commit 见本轮。 |
| REQ-023 | 2026-09-11 | 修复号型规格系列设定「编辑变新增」bug：设置页「号型规格系列设定」中点任意现有系列的编辑按钮（✏️），保存后不是更新原系列而是新增一条同名记录，列表不断产生重复行。根因（2026-09-11 读码确认）：前端 src/api/index.js `saveSizeGroups` 永远 POST `/api/size-groups`；后端 routes/sizeGroups.cjs POST 永远调 service.create()（INSERT 新行，忽略请求体 id）——虽已存在 PATCH `/api/size-groups/:id` + service.update() 路由，前端却从未调用；SizeGroupManager 编辑按钮 setEditing(g) 已带 id、弹窗标题也按 id 区分编辑/新增，组件层无 bug。期望：编辑现有系列走更新语义——前端按 editing.id 区分（有 id 调 PATCH /api/size-groups/{id}，无 id 才 POST 新增），或后端 POST 按 id upsert；编辑保存后原系列被更新、列表不新增重复行；并清理该 bug 此前误操作已产生的重复数据（截图中「女装号型（上装）」与「女装」size_list 完全相同，疑似重复）。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_023.png（号型系列列表：女装号型/男士衬衫号型/女装号型（下装）/女装号型（上装）/女装，末行「女装」与上一行 size_list 完全相同，疑为 bug 产生的重复）；涉及 src/components/settings/SizeGroupManager.jsx、src/api/index.js:53、server/routes/sizeGroups.cjs、server/services/sizeGroups.cjs；与 REQ-013（同页新增对话框复核正常）同区；修复后需回归新增/编辑/删除三路径，并确认旧重复行清理不影响已绑定分类的尺码关联。**实施（2026-09-11）**：①前端 src/api/index.js 新增 `updateSizeGroup(id, data)`（PATCH `/api/size-groups/{id}`，后端路由/service 本已存在）；②SizeGroupManager.save 按 editing.id 区分——有 id 调 PATCH 更新原系列、无 id 才 POST 新增；③清理 bug 重复行——数据库核对 size_groups 6 行（id1-6），id6「女装」与 id5「女装号型（上装）」size_list 完全相同且无任何分类绑定（settings.categories 绑定：连衣裙→2/夹克→1/外套→1/风衣→2/衬衣→2/半裙→1/裤子→4），判定为编辑改名误新增的重复行，已 DELETE id6 清理，其余 5 行不动。**验证（2026-09-11）**：浏览器 E2E（设置页进入→编辑「女装号型（上装）」改名→保存→列表行数不变（5→5）且原行更新→改回原名恢复无残留）+ 删除 id6 后列表 5 行正常；回归新增/编辑/删除三路径通过；分类绑定关联（size_group_id）未受影响，commit 见本轮。 |
| REQ-024 | 2026-09-11 | 看板各分组视图均与实际情况对不上，已无法正常使用，先增加一个「无过滤/全部」视图作为兜底：用户判断看板整体分组逻辑已乱（任务状态/版次进度/紧急程度/逾期情况每种关注点都对不上实际数据），后续需整体重新梳理看板分组口径（另立专项，不在本条范围）。本次先落地：关注点下拉新增一项「全部」视图——单栏展示全部打样单，不做任何分组，顶部数据检索过滤（款号/品类/版次/设计师/优先级）仍可叠加，让用户先能完整、不挑拣地看到所有款。截图佐证（docs/archive/req-screenshots/_req_screenshot_024.png）：当前「关注点：任务状态」视图下「待处理」列 0 张卡（红框空列）、「打版中」3、「已完结」3；单卡内多版次状态混排（26AWW526：胚样已完成/头版样样衣中/复版一待配料；235-6：胚样已完成/头版样打版中，红框标注），一张卡横跨多个状态，分栏口径与实际不符。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_024.png（右上角=关注点选择器；红框=待处理空列、26AWW526 复版一待配料行、235-6 头版样打版中行）；涉及组件 src/components/task/KanbanView.jsx（getActiveCols 增加 'all' 分支单列渲染、kanbanGroupBy 选项增加「关注点：全部」）；与 REQ-022（进度口径已改最先进批次）衔接——「全部」视图不得受任何分组字段误过滤；看板整体分组逻辑重构另立专项跟进。**实施（2026-09-11）**：KanbanView getActiveCols 新增 `all` 分支（单列「全部」）、分组过滤 `kanbanGroupBy==='all'` 直接放行（不按状态/版次/优先级/逾期过滤）、关注点下拉新增「关注点：全部」选项；顶部数据检索过滤（款号/分类/版次/设计师/优先级）照常叠加。**验证（2026-09-11）**：浏览器 E2E 断言单栏 1 列 + 全部 6 款 + 截图目检（「关注点：全部」单栏滚动展示 6 卡）+ 生产构建通过，commit 见本轮。**2026-09-11 用户验收修订（本条重开：已完成→待开发）**：单列实现浪费横向空间——docs/archive/req-screenshots/_req_screenshot_026.png 实证「关注点：全部」视图卡片全部挤在左侧 400px 单列内，右侧约 60% 屏宽空白（红框）。期望：「全部」视图改为自适应多列布局——按视口宽度自动排 N 列（CSS grid auto-fill/auto-fit 或等价方案），卡片宽度随列数自适应，不固定单列，充分利用横向空间；其余视图（按状态/版次/优先级/逾期分列）维持既有列宽不变。 | **2026-09-11 重开重做（已落地）**：看板主体改三元分支——kanbanGroupBy==='all' 时卡片直排 CSS grid repeat(auto-fill, minmax(360px,1fr)) 自适应多列（卡片宽度随列数自适应，实测 1440px 视口 3 列/卡宽约 443px），其余分组视图维持 400px 列宽；renderBentoCard 提取为共用函数（全部/分组视图同卡渲染）。**验证（2026-09-11）**：E2E 断言容器 display=grid、6 卡、首行 3 列、卡宽 443px（380-700 区间）+ 截图目检（3 列铺满横向空间）+ 生产构建通过，commit 见本轮。
| REQ-025 | 2026-09-11 | 修复看板「任务状态」分栏把进行中款错分进「已完结」：docs/archive/req-screenshots/_req_screenshot_025.png 实证——「已完结」栏（计数 3）中，26AWW526（褶皱金属丝棒球服，MS.H）版次条为 胚样已完成/头版样样衣中/**复版一待配料**（红框）、235-6（中国，未分配）版次条为 胚样已完成/**头版样打版中**（红框），两款均有进行中版次却归入已完结列。根因方向（读码 KanbanView.jsx 核对）：状态分栏用 normalizeStatus(task.status) 读款级存储字段（todo/doing/done），而 REQ-022 已拍板进度权威=最先进批次；款级 task.status 系历史遗留/不同步（批次状态变更后未重算或旧脏值），导致活跃款落入已完结。期望：分栏口径与 REQ-022 一致——按最先进批次状态实时推导款级分栏（done/doing/todo），不再依赖可能过期的 task.status；并对存量错栏数据重算归位（26AWW526/235-6 及同类款）。 | P1 | 已完成 | 参考截图：docs/archive/req-screenshots/_req_screenshot_025.png（已完结栏内红框=26AWW526 复版一待配料行、235-6 头版样打版中行）；涉及 src/components/task/KanbanView.jsx（normalizeStatus/分栏过滤）、后端 tasks.cjs（款级 status 同步/deriveStyleStatus/findTopRun 口径需与看板一致）；与 REQ-022（进度权威=最先进批次）、REQ-024（全部视图兜底）同属看板逻辑修复链；修复后验证：待处理/打版中/已完结三列无「有进行中版次却在已完结」的款。**实施（2026-09-11）**：口径修正＝**完成=该款全部批次均已完成**，否则取未完成批次中最先进（延续 REQ-022 最先进批次权威，排除已完成批次）：①后端 tasks.cjs deriveStyleStatus 改「active=未完成批次，无则 done」；findTopRun 改「未完成批次中最先进，全 done 回退首个批次」（top_run 投影=当前进行中批次，26AWW526 版单从 PO-V0 改显示 PO-V1 头版样）；②sampleRuns.cjs syncTaskStatus 同步新口径；③index.cjs 启动时 recalcAllTaskStatus() 存量重算（幂等）——26AWW526/235-6 款级 status done→doing 归位；④前端 KanbanView 新增 derivedCol(t)（derived_status→todo/doing/done），状态分栏/列表状态列改用 derivedCol 实时推导，不再读可能过期的 task.status；getOverdueInfo 完成判定同步改 derived_status==='done'。**验证（2026-09-11）**：HTTP 确认 6 款款级状态重算归位（26AWW526/235-6 doing，AW26-JK001 全 done 保持 done，DR002/TS003/PA004 doing）+ 浏览器 E2E（26AWW526/235-6 归「打版中」列且不在「已完结」、DR002/TS003 不在已完结、AW26-JK001 在已完结、零 console 错误）+ 截图目检（打版中 5 款、已完结 1 款）+ 生产构建通过，commit 见本轮。 |
| REQ-026 | 2026-09-11 | 在数据库中重写各品类测量部位数据：以 garment-tech-partner 技能权威数据为准，重写 measurement_templates 表各品类测量部位（部位名/测量方法/公差/放码规则/是否必填）。现状（2026-09-11 实查 server/database.sqlite）：全表仅 10 行——上装 5、半裙 1、毛衫 1、针织上装 2、针织下装 1；外套/连衣裙/裤装/衬衫等实际使用品类完全无预设（REQ-005 曾报「外套」品类预设 rows=0）；且数据质量差——半裙/毛衫/针织下装各仅 1 个部位、公差格式混乱（±1 / (±) 1 / 0.5 / (±) 1.2 并存）、code 混乱（0 / 01 / 胸围 / 02 混用）。期望：按技能 references（01-06 品类工艺标准 + 08 质检验收测量方法 + 11 尺寸放码参考）为每个实际品类补齐完整测量部位清单，统一 code 规则、公差写法、测量方法，覆盖上装/外套/连衣裙/裤装/半裙/针织上下装/衬衫/毛衫等；重写时保护已建款式尺寸表（按 code 引用的历史数据不受影响）。 | P1 | 已完成 | 现状实查（ELECTRON_RUN_AS_NODE 只读查询）：measurement_templates 字段 category/code/name/method/tolerance/grading_rule/sort_order/is_required，共 10 行；新增款建批次时 sampleRuns.cjs presetSizeData 按品类取该表生成初始尺寸表（无品类则空表）；与 REQ-005①（尺寸表归属版次、从品类预设复制）直接相关；权威数据源=garment-tech-partner 技能 references/01-06/08/11，不自行脑补部位。**拍板（2026-09-11 用户，经澄清）**：重写方式＝**覆盖式重写**；按技能 references 权威数据**正常写各品类测量部位即可**，**无需特别处理/兼容现有款式尺寸数据（用户明确"不用理会现有款数据"）**；不等用户另供资料，数据源即以技能 references 为准。 | **实施（2026-09-11）**：读技能 references/01-06（品类部位）、08（测量方法/公差基线）、11/14（档差）权威数据 → 构造 97 行/11 品类覆盖式重写（上装11/外套9/夹克9/风衣9/连衣裙11/裤子10/半裙7/衬衣8/针织上装7/针织下装9/毛衫7；code 01.. 规范；公差基线 衣长/袖长/肩宽/裤长/裙长 ±0.5、胸围 ±1、腰围 ±0.8、其余 ±0.3；档差 胸/腰/下摆 4、肩宽/袖长/袖口/领围 1、前浪 0.5、后浪 0.7、横裆 2.2、中裆 2、脚口 2/1.8/1.6）→ electron node 模式事务写库（备份 server/database.backup_before_req026.sqlite）。**验证（2026-09-11）**：GET /api/measurement-templates 全表 97 行、?category=外套 9 行正确返回；按用户拍板不处理现有款数据。**2026-09-11 修复（用户报告下装未写入）**：数据本已入库但品类命名三处错位——设置页「尺寸部位管理」目录读 settings.measurementCategories（原仅 5 项且含历史品类「下装」），而模板权威品类为裤子/半裙/针织下装等，目录点不到下装类、存量下装款（AW26-PA004 category=「下装」）预设取空。修复：①settings.measurementCategories ← 11 模板品类（上装/外套/夹克/风衣/连衣裙/裤子/半裙/衬衣/针织上装/针织下装/毛衫）；②settings.categories 补齐 上装→5/针织上装→5/针织下装→4/毛衫→5（新建款可选全品类）；③styles 存量「下装」→「裤子」（PA004）；④sample_runs run6（PA004 复版二）size_data 补裤子预设 10 行。**验证（2026-09-11）**：设置页 11 品类目录、裤子 10/半裙 7/针织下装 9/上装 11/毛衫 7 行可见；新建表单品类 11 项；PA004 详情品类=裤子、尺寸表渲染 10 行裤子部位（裤长/腰围/臀围/前浪/后浪/横裆/中裆/脚口/腰头宽高/裤袢长宽）；E2E 14 项全过 + 截图目检，commit 见本轮。
| REQ-027 | 2026-09-11 | 修复「当前进度」取错版次：26AWW526 最新版次「复版一」状态为待配料，整款却被分到「打版中」列（docs/archive/req-screenshots/_req_screenshot_027.png：版单 PO-26AWW526-V1，版次条 胚样已完成/头版样样衣中/复版一待配料（红框），卡片在「打版中」列）。根因（读码 tasks.cjs 核对）：REQ-025 定的 findTopRun/deriveStyleStatus 在未完成批次中取**状态 rank 最高**者——26AWW526 未完成批次＝头版样(样衣中 rank 高)+复版一(待配料 rank 低)，于是取了旧版次头版样。业务口径错误：**开新版次（复版一）意味着从头重做，当前进度应以最新版次（复版一）为准**，旧版次的样衣中/打版中是历史进度，不代表当前在干什么。期望：当前进度权威改为**未完成批次中 sort_order 最大者（最新版次）**，而非状态 rank 最高者——复版一待配料 → 26AWW526 应在「待处理」列；全部版次均完成才进「已完结」。findTopRun/deriveStyleStatus/top_run 投影/逾期基准/底部进度节点口径同步修正，存量重算归位。 | P1 | 已完成 | 截图 docs/archive/req-screenshots/_req_screenshot_027.png（「打版中」列 5 卡中 26AWW526 复版一待配料红框）；涉及 server/services/tasks.cjs（deriveStyleStatus:62、findTopRun:77 的 RUN_STATUS_RANK 取最大逻辑）、sampleRuns.cjs syncTaskStatus、KanbanView derivedCol/逾期判定；与 REQ-022（逾期基准=最先进批次预期）、REQ-025（取最先进批次）同口径链——本条是对「最先进」定义的修正（按版次新旧而非状态 rank）；修复后验证：26AWW526 归「待处理」列且复版一待配料为当前版，235-6（胚样完成+头版样打版中、无更新版次）仍在打版中，其余款不串栏。 | **实施（2026-09-11）**：tasks.cjs deriveStyleStatus 改 active=status!=='done' 取 sort_order 最大者的 status（无 active→done）、findTopRun 改 active 空回退全部 runs 取 sort_order 最大；sampleRuns.cjs syncTaskStatus 同步（SELECT status, sort_order）；重启（PMProDev）后 HTTP 验证 6 款归位（26AWW526→todo/复版一 waiting_material/版单 PO-26AWW526-V2、TS003→todo/生产样 waiting_material/预期 2026-07-10、235-6→doing 保持、AW26-JK001→done 保持）。**验证（2026-09-11）**：E2E 断言 26AWW526/TS003 归「待处理」、235-6/DR002/PA004 在「打版中」、AW26-JK001 在「已完结」、无串栏 + 截图目检（待处理2/打版中3/已完结1）+ 生产构建通过，commit 见本轮。
| REQ-028 | 2026-09-11 | 修复看板列标题宽度与卡片宽度不一致：docs/archive/req-screenshots/_req_screenshot_028.png 实证——「打版中」「已完结」列的列标题栏（列名+计数）明显窄于下方卡片，卡片右缘（款名/版单信息区）探出列头右侧，左右不对齐（红框圈出整张卡，卡头「打版中 5」条宽度小于卡片宽度）。期望：同一列内列标题栏宽度与卡片宽度严格一致（同宽对齐，卡片不溢出列头），各列自身列头=卡片同宽；不改动卡片内容与信息密度。 | P2 | 已完成 | 截图 docs/archive/req-screenshots/_req_screenshot_028.png；涉及 src/components/task/KanbanView.jsx 列容器/列头样式（列宽与卡片宽度不同源导致错位，应统一定宽）；与 REQ-024（全部视图自适应多列）同属看板布局，改列宽方案时一并保证列头=卡片同宽。 | **实施（2026-09-11）**：列头 width:400 → width:'100%' 继承列宽（与卡片同源同宽），.col-body 去 padding-right；与 REQ-024 重开一并落地。**验证（2026-09-11）**：E2E 断言各列列头宽度=卡片宽度（600=600×3）+ 截图目检（列头与卡片右缘对齐）+ 生产构建通过，commit 见本轮。
| REQ-029 | 2026-09-11 | 修复看板数据不随返回刷新：在详情页改完数据（如批次预期完成时间、版次状态）后返回看板，看板仍显示旧数据，必须手动刷新（F5）才会更新——例：生产样预期交期由 2026-03-25 改为 2026-05-15 后，返回看板仍显示「逾期 170 天 / 03-25」，手动刷新后才变 119 天。根因（读码 App.jsx 核对）：详情页返回 onBack 只做 setView('kanban')（App.jsx:306），不调 loadTasks；批次表单即改即存已落库，但看板内存中的 tasks 列表在视图切换时不重新拉取（仅首次挂载 App.jsx:127 与新建/删除后拉取）。期望：从详情页返回看板（及看板从其他视图切回）时自动重新 loadTasks，看板卡片的分栏/状态/逾期天数/进度节点与最新落库数据一致，无需手动刷新；不改动即改即存逻辑本身。 | P1 | 已完成 | 无新截图（承接 REQ-027 截图的 TS003 交期问题）；涉及 src/App.jsx（onBack/视图切换处补 loadTasks，或 KanbanView 挂载/切回时 reload）；与 REQ-022（逾期角标读 top_run.expected_date）直接相关——数据不刷新时逾期角标显示旧值，用户误以为算法错；修复后验证：详情页改交期/状态→返回看板即时反映，无需 F5。 | **实施（2026-09-11）**：App.jsx 新增 prevView=useRef(view) + useEffect 检测 view==='kanban' 且 prevView!=='kanban' 时 loadTasks()（插在初始加载 useEffect 后），覆盖详情页返回及其他视图切回看板的所有路径；不改即改即存。**验证（2026-09-11）**：E2E 详情页生产样预期交期 07-10→09-30→返回看板 TS003 角标「逾期 63 天」即时消失（未 F5）→改回 07-10 恢复 + 零 console 错误，commit 见本轮。
| REQ-030 | 2026-09-11 | 文案改词与优先级体系替换：①版次状态选项改词——「待配料」→「待安排」、「待确认」→「待审版」（截图 docs/archive/req-screenshots/_req_screenshot_029a.png：状态下拉当前 待配料/打版中/样衣中/待确认/已完成，待配料、待确认两项改名；打版中/样衣中/已完成不变）；②优先级体系整体替换——现「低/中/高/紧急」（截图 docs/archive/req-screenshots/_req_screenshot_029b.png，当前值=紧急）改为 **S / A / B / C** 四级。期望：全系统所有出现处（批次状态下拉、看板分栏与版次条文案、优先级下拉、看板「紧急程度」关注点分组、逾期/节点等所有文案）统一替换；存储 key 建议不动（waiting_material/pending_confirm 仅改展示 label），优先级历史数据需迁移映射（按 S 最高 C 最低推断：紧急→S、高→A、中→B、低→C，最终映射开发时与用户确认），迁移后历史款优先级显示正确。 | P2 | 已完成 | 截图 docs/archive/req-screenshots/_req_screenshot_029a.png（状态下拉）、029b.png（优先级下拉 低/中/高/紧急）；涉及 RUN_STATUS_META 文案、SampleRunList 状态/优先级 SmartSelect 选项、KanbanView 列名/版次条/「紧急程度」分组列、相关过滤筛选；与 REQ-027（版次状态驱动分栏）同链——改词不改排序逻辑；优先级色（原紧急红/高高亮等）随新四级重配；修复后验证：下拉无旧词「待配料/待确认/低中高紧急」、历史款优先级映射后显示 S/A/B/C 正确。 **实施（2026-09-11）**：①版次状态改词——waiting_material「待配料」→「待安排」、pending_confirm「待确认」→「待审版」（key 不变仅展示 label，KanbanView/SampleRunList/DesignerDashboard 三处 RUN_STATUS_META + 仪表盘统计卡「待安排/未开始」「待审版」同步）；②优先级体系替换——低/中/高/紧急 → C/B/A/S（S 最高）：KanbanView PRIO_RANK 改 S/A/B/C、taskTopPriority 默认 B、优先级分组列 S/A/B/C（S 红/A 橙/B 品牌色/C 中性）、过滤栏与新建弹窗选项 C/B/A/S、「关注点：紧急程度」→「关注点：优先级」、批次卡「优先」值色 prioColor（S 红/A 橙/B 蓝/C 中性）、prio-high/mid/low 类映射 S→high/A→mid/B·C→low；③后端默认优先级 中→B（sampleRuns.cjs）；④迁移 v20 存量映射 sample_runs+tasks：紧急→S、高→A、中→B、低→C（实测：sample_runs 高1→A、低1→C、紧急1→S、中7→B；tasks 款级 紧急→S(235-6)、高→A(JK001)、其余 B）。**验证（2026-09-11）**：E2E 13/13 PASS（看板/详情/仪表盘无「待配料/待确认」、过滤栏优先级=全部+C/B/A/S、关注点优先级分组出现 S/A 列（235-6=S、JK001=A）、详情优先级面板 S/A/B/C 无旧词、新建弹窗 C/B/A/S、仪表盘「待安排/未开始」「待审版」、零 console 错误）+ 优先级分组/面板截图目检 + 生产构建通过（10.04s），commit 见本轮。 |
| REQ-031 | 2026-09-11 | 数据检索过滤栏新增「版次状态」筛选条件：docs/archive/req-screenshots/_req_screenshot_030.png 实证——顶部数据检索过滤栏现为 款号/款名搜索｜全部分类｜全部打样版次｜全部分派设计师｜全部优先级，末尾红框空位待加筛选。期望：在优先级下拉后新增「版次状态」下拉（默认=全部版次状态），选项=各版次状态（待安排/打版中/样衣中/待审版/已完成，与 REQ-030 改词后一致），选中后只显示**存在处于该状态版次**的款；与现有分类/版次/设计师/优先级/搜索框过滤叠加生效，不影响「关注点」分组列本身。 | P2 | 已完成 | 截图 docs/archive/req-screenshots/_req_screenshot_030.png（红框=新增筛选位，位于全部优先级下拉右侧）；涉及 KanbanView.jsx 过滤栏与过滤逻辑（现有版次/设计师/优先级过滤同模式）；与 REQ-030（状态改词）联动——选项文案随改词；与 REQ-027（当前版次口径）区分：本筛选按「是否存在该状态的版次」还是「当前版次状态=所选」开发时定，建议按当前版次（最新未完成版次）过滤以与看板分栏口径一致。 **实施（2026-09-11）**：KanbanView 过滤栏优先级下拉后新增「全部版次状态」下拉（选项=待安排/打版中/样衣中/待审版/已完成，经 RUN_STATUS_META key+label 生成）；filterTasks 新增 run_status 存在性过滤（taskRuns 任一批次 status 匹配即放行，与分栏口径解耦）；与现有 款号搜索/分类/版次/设计师/优先级 过滤叠加，不影响关注点分组。**验证（2026-09-11）**：E2E 11/11 PASS（下拉存在且选项正确、选「打版中」仅剩 235-6/AW26-PA004 2 卡且不含全完成的 AW26-JK001、清空恢复 6 卡、零 console 错误）+ 截图目检（过滤栏含全部版次状态）。**2026-09-12 重开修复（验收驳回后）**：过滤栏「全部版次状态」由原生 select 替换为全站统一 SmartSelect 组件（filter-run-status 类）——深色圆角触发框（min-height 36/圆角 8/字体 13，与其它过滤控件同高）+ 深色圆角下拉面板（bg-elev-2/border-strong/12px 圆角/选中 accent-soft 高亮，与批次优先级 S/A/B/C 面板同款），选项=RUN_STATUS_META 枚举 {key,label}（待安排/打版中/样衣中/待审版/已完成）、allowCustom=false（无「手动输入自定义值」行）、保留「预设选项」分区；筛选功能（存在性过滤）原样保留。**验证（2026-09-12）**：E2E 17/17 PASS（存在 .filter-run-status 且内部 0 原生 select、触发框 36px/8px、展开面板深色 rgb(30,32,35) 亮度 97<400/12px 圆角、无自定义输入行、有预设选项分区、选项 5 项正确、选「打版中」仅剩 235-6/AW26-PA004 恰好 2 卡、切「待安排」得 26AWW526/SS26-TS003、触发框选中态显示「待安排」、零 console 错误）+ 面板截图目检（深色圆角、5 选项）+ 生产构建通过（9.80s），commit 见本轮。 |
| REQ-032 | 2026-09-11 | 看板卡片最小宽度不得低于 500px：docs/archive/req-screenshots/_req_screenshot_031.png 实证——「全部」自适应网格（REQ-024 落地为 minmax(360px,1fr)）在屏宽有限时把最右张卡压到约 360–400px，款号（26AWM…/SS26-D…）、品牌（ANNA J/GOLDEI）等字段挤成省略号、版面拥挤。期望：看板所有卡片（「全部」自适应网格及分组视图列内卡片）最小宽度**不得低于 500px**——网格 minmax 下限改 500（minmax(500px,1fr)），列宽/卡片宽度同步下限 500；宽度不足时减少列数换行，而不是把卡片压到 500 以下；不改卡片内容与信息密度。 | P2 | 已完成 | 截图 docs/archive/req-screenshots/_req_screenshot_031.png（红框=被压窄的 SS26-DR002 卡，款号/品牌截断）；涉及 KanbanView.jsx「全部」视图 grid minmax(360px→500px) 及分组视图列宽/卡片 min-width；与 REQ-024（自适应多列）直接联动、与 REQ-028（列头=卡片同宽）联动；验证：任意视口下卡片实测宽度≥500px，款号/品牌不被挤压截断。 **实施（2026-09-11）**：全部视图 grid minmax(360px→500px,1fr)（卡片最小宽度 500px，宽度不足自动减列换行）；分组视图列宽 400→500（.col width/flex-basis 同步 500）。**验证（2026-09-11）**：E2E 11/11 PASS（1440px 视口全部视图 6 卡全 ≥500px 实测 676px、grid 模板 minmax(500px,1fr)、分组视图 3 列全 ≥500px 实测 600px、款号/品牌无截断）+ 截图目检（3 列铺满、款号完整）+ 生产构建通过（9.87s），commit 见本轮 |
| REQ-033 | 2026-09-15 | 看板卡片布局重构专项（批4 U18「两层层级」方案复盘 + 与 REQ-032 最小宽度红线冲突裁决）：批4 U18 把卡片改为「主信息层常驻 + 次级详情层 hover 浮层」，**原目标「同屏卡片≈+35%」已达成**（卡片高度约 320→238），但验收反馈「改动较大、观感并不理想」，暴露出两个问题：①**信息获取成本上升**——品牌 / 时段 / 版单 / 批次明细从常驻改为 hover 浮层（0.25s 延迟）才可见，扫板时看得到的信息变少；②**U22 响应式断点越过了 REQ-032 的红线**——分组视图列宽 `--board-col-w` 被降到 420/380/360px（≤1366/1200/1024），而 REQ-032（2026-09-11 已拍板）明确「看板卡片最小宽度不得低于 500px（列宽/卡片宽度同步下限 500）」；窄列连带引发款号字号被迫 24→20→18px、逾期徽标与款号被迫垂直分层、meta 行拥挤等一串补丁。期望：重新决策卡片信息架构与尺寸基线——(a) 明确**常驻信息集**（款号/款名/优先级/款级状态/进度节点为底，品牌/时段/版单/批次明细是否回归常驻待拍板）；(b) **裁决 500px 下限与「笔记本各宽度可用」的冲突**（维持 500px 走横向滚动 / 下调下限并明确放宽谁 / 缩略图缩尺换密度）；(c) U18/U19 的组件分层与 memo、U20 懒渲染队列基建**不回退**。范围：`src/components/task/TaskCard.jsx`、`KanbanView.jsx`、`src/styles/components.css`、`views.css`、`theme.css`（`--board-col-w`）。 | P2 | 待开发 | 复盘结论（2026-09-15）：U18 的**代码级收益是真的**（KanbanView→TaskCard 容器分层、`utils/taskView.js` 单一来源、U19 memo/useMemo/乐观更新、U20 缩略图并发队列），建议保留；争议只集中在「常驻 vs 按需」的信息架构与列宽下限。用户 2026-09-15 指示：先记录、跳过本项，另立专项再动。关联：REQ-024 / REQ-028 / REQ-032（看板布局链）；REQ-022 / REQ-025 / REQ-027（进度口径链，本专项不涉及、不受影响）。 |
| REQ-034 | 2026-09-15 | 安装包与开发环境共用端口 3001 致后端/数据互相干扰（"共库"假象）：安装包后端与渲染器硬编码 `localhost:3001`（server/index.cjs:116 / main.js:154），开发环境 `npm run dev:all` 后端也占用 3001（读 `D:\dev\golden-shuttle\server\database.sqlite` 真实库）。若测安装包时开发环境在跑，安装包窗口指向的 3001 实际由开发后端服务→看到开发数据，误以为"共库"；且 `app.listen` 无 EADDRINUSE 处理，端口被占会抛未捕获异常致安装包崩溃而非优雅降级。实测：安装包实际用独立空库 `AppData\Roaming\golden-shuttle\database.sqlite`（118KB 空白种子拷贝），并未读开发库（270KB 真实数据），故"共库"实为端口耦合假象。修复方向：Electron 单实例锁 + 端口冲突优雅降级/动态端口，安装包始终用自身隔离库，且不与其他实例抢 3001。 | P2 | 待开发 | 关联 REQ-033（同批搁置）；根因排查见 main.js:81 `isDev=!app.isPackaged` 分支与 server/index.cjs:116。2026-09-15 用户实测 win 安装包后提出，主理人查证登记。 |
| REQ-035 | 2026-09-15 | 安装包首次运行初始化：全新安装启动后需增加首次运行设置过程——①**品牌名称**：用户输入默认品牌名（影响款式默认品牌与导出表头等，具体联动开发时确认）；②**数据库存储位置**：用户选择数据库文件存放位置（默认 `%APPDATA%/PatternMaster Pro/database.sqlite`，可自定义路径）。验收：首次启动弹出初始化引导 → 完成品牌名称与数据库位置设置 → 进入主界面；设置持久化，二次启动不再弹出；数据库创建在用户指定位置且读写正常。 | P1 | 待开发 | 关联 REQ-034（安装包体验完善同批）；现状 main.js 首次启动从 resources 拷贝空白示例库到 %APPDATA%、无任何初始化设置；范围 main.js / preload / 新初始化向导组件 / settings 存储；品牌名称是否参与导出（工艺单 Excel/PDF）与款式默认值开发时确认。 |
| REQ-036 | 2026-09-15 | 窗口关闭按钮行为可选：点击右上角 X 是「直接关闭退出」还是「最小化到任务栏（托盘）继续运行」，需要给用户选择。期望：设置页新增「关闭按钮行为」选项（直接退出 / 最小化到任务栏），默认直接退出（与现状一致）；选最小化时点击 X 隐藏主窗口到系统托盘，提供托盘图标与托盘菜单（恢复窗口 / 退出）。 | P2 | 待开发 | 范围 main.js（close 事件拦截 + Tray 托盘）+ 设置页 SettingsView 新增项；与 REQ-034 单实例锁联动考虑；默认值待用户拍板（建议默认直接退出，行为不变）。 |
| REQ-037 | 2026-09-15 | 设置页「尺寸部位管理」部位预设明细表**数据穿透**（用户 2026-09-15 定义：数据行/列滚动时会在标题栏或首列边缘显示出来）：表格纵向滚动时，滚过标题栏的数据行未在表头处被裁剪、穿透显示——截图 docs/archive/req-screenshots/_req_screenshot_037.png 红框（覆盖胸围~袖长行）实证：首个数据行「胸围」出现在表头行（部位名称/测量方法说明/…）上方。期望：纵向滚动时滚出表头区域的数据行在标题栏处被裁剪隐藏，不穿透显示。 | P1 | 待开发 | 截图 037（红框=穿透区域）；涉及 MeasurementTemplateManager 预设明细表 sticky 表头/裁剪（z-index、overflow）；与 REQ-038 同根因统一排查。 |
| REQ-038 | 2026-09-15 | 看板列表视图**数据穿透**（与 REQ-037 同类，用户定义：数据行/列滚动时会在标题栏或首列边缘显示出来）：横向滚动时，数据列在首列（版次列）边缘穿透显示、未被裁剪——截图 docs/archive/req-screenshots/_req_screenshot_038.png 左侧红竖线标注首列边缘。期望：横向滚动时滚过首列的数据列在首列边缘被裁剪隐藏，不穿透显示。 | P1 | 待开发 | 截图 038（红竖线=首列边缘）；涉及 KanbanView 列表视图 sticky 首列/裁剪；与 REQ-037 同根因统一排查。 |
| REQ-039 | 2026-09-15 | 看板**按钮文字换行**：看板右上「关注点：任务状态」按钮/下拉面板文字折行——截图 docs/archive/req-screenshots/_req_screenshot_039.png 红框实证：面板文字折成「关注点:/任务/状态」三行（「任务状态」折为两行）；同处红框的「全部分派设计师」筛选框是否连带异常一并复核。期望：按钮文字单行完整显示、不换行。 | P2 | 待开发 | 截图 039（红框=换行区域）；涉及看板关注点分组按钮/下拉面板宽度；「全部分派设计师」筛选框实施时现场复核。 |
<!-- 模板说明（新增需求时删除本行并按此填写）：
- 编号：REQ-001 起递增
- 日期：提交日期，YYYY-MM-DD
- 需求描述：功能 / 改动点，写明验收标准（怎么做算完成）
- 优先级：P0（必须做）/ P1（应该做）/ P2（可以做）
- 状态：见上方图例，默认「待开发」
- 备注：关联文档、依赖、约束、上下文链接等
-->
