# 批 3 · UI 治本 —— 锚点侦察（只读）

> 来源：software-architect-2-2 只读侦察（team `software-golden-shuttle-ui`）。
> 侦察过程**未改动任何文件、未执行任何 git 命令**；已排除 `dist/**`。
> 本文件作为 U1–U12 施工单的锚点依据。U7（CSS 分层/冲突/!important）由另一路单独输出。

## 0. 基线复核（与主理人实测一致）

| 项 | 实测 | 说明 |
|---|---|---|
| `src/index.css` | 1424 行 / 156 个 `!important` | ✓ |
| `src/styles/app.css` | 685 行 / 23 个 `!important`（分布在 12 行） | ✓ |
| `src/styles/theme.css` | 133 行 / 0 个 `!important` | ✓ |
| `src/App.css` | 42 行，**零引用** | `grep -rn "App.css" src/` 零命中（仅 docs 命中） |
| 入口样式 | `src/main.jsx:3-5` 只 import `index.css` + `styles/theme.css` + `styles/app.css` | **未引入 App.css** |

---

## A. 批 1 遗留（U1–U6）

### U1 解除 1280 硬约束
- 现状：`App.jsx:264` `overflow:'hidden'`；`App.jsx:265` `minWidth:1280`；整块内联样式在 `App.jsx:258-266`。
- 同类约束排查：
  - `html`/`body` **无** overflow 规则（`index.css:13-24` 仅 margin/padding/font/background-image/min-height）→ **不是裁切来源**。
  - `#root` 规则只在 `App.css:1-6`（`max-width:1280px`）→ **死规则**（文件未引入）。
  - `app.css:68` `.template-manager-v4 { min-width: 800px }` ← 设置页次级硬约束，**本批不动**，由根容器 `overflow-x:auto` 兜底滚动（U7/U22 再流体化）。
  - `KanbanView.jsx:480`（`width:500; flex:'0 0 500px'`）与 `index.css:1148-1152`（`.col{min-width:600px!important;flex:0 0 600px!important}`）**两处冲突** → 登记给 U7/U18。
- 裁切方向：LTR 左起 0 + `overflow:hidden` ⇒ **右侧被裁**。1366×768@125% ⇒ 视口≈1093px，裁 ≈187px、**无滚动条**。被裁内容 = 顶栏 `.header-ops-v4`（`DetailView.jsx:169-213`：导出工艺单/导出 PDF/历史版本/删除单据）+ 看板最右列。1920×1080@150% ⇒ ≈1280px，正卡临界。

### U2 文字令牌 + placeholder 二次衰减
各主题当前取值：

| 令牌 | custom | dark | light |
|---|---|---|---|
| `--text` | `#f0ece6` theme.css:19 | `CanvasText` :54 | `CanvasText` :88 |
| `--text-2` | `#9d9890` :20 | `#b4bcc8` :55 | `#3d4757` :89 |
| `--text-3` | `#6b6560` :21 | `#8f99a6` :56 | `#5c6675` :90 |
| `--text-4` | `#4a4540` :22 | `#67707e` :57 | `#8b92a0` :91 |

对比度实测（custom 底 `--bg #0e0f11`；dark/light 底取 `#1e1e1e`/`#fff` 估算，标“≈”）：
- custom：`--text-3` ≈**3.34:1**（对 `--bg-elev` ≈3.0）；`--text-4` ≈**2.02:1** → 复现审计 3.3 / 2.0
- dark：`--text-3` ≈5.8:1 达标；`--text-4` ≈3.3:1 缺
- light：`--text-3` ≈5.8:1 达标；`--text-4` ≈3.1:1 缺
- ⇒ 痛点集中在 **custom 的 text-3/text-4**。

建议新值（custom，与审计一致）：`--text-3: #8c857c`（对 `#0e0f11` ≈5.2:1、对 `#161719` ≈4.9:1，均 ≥4.5）；`--text-4` 收敛为「分隔/装饰」语义。

**placeholder 二次衰减 —— 只有一处**：`app.css:660`
`.data-table input/textarea::placeholder { color:var(--text-4); opacity:.5 }` → 合成 ≈`#2c2a28`、≈1.35:1。
其余 placeholder 均**无**二次衰减，只有 `color:var(--text-4)`：`app.css:215`（.field）、`app.css:226`（.ss-display .placeholder）、`app.css:583`（.dp-placeholder，DatePicker）、`index.css:1422`（.grid-cell）。

全站消费点：`var(--text-3)` **87 处 / 19 文件**（含死文件 `App.css:41`）；`var(--text-4)` **29 处 / 8 文件**。

### U3 找回氛围光
- 覆盖行：`App.jsx:262` 内联 `background:'var(--bg)'`（内联 > 类，压掉 `app.css:7` 的 `var(--app-bg)`）。
- `--app-bg`：`theme.css:39`（custom = 两条金色 `<200,169,110>` radial-gradient）；`theme.css:74` dark = `var(--bg)`；`theme.css:108` light = `var(--bg)`。
- `body` 另有脚手架残留渐变：`index.css:19-23`（三条**紫/玫瑰色** `hsla(253…)/hsla(225…)/hsla(339…)`）——被 `.app`(100vh) 覆盖不可见，属隐患，建议随 U7 清理。
- 判定：删 `App.jsx:262` 后 custom 金色氛围光**能生效**；dark/light 仍纯色（符合 theme.css:123-133「深浅主题零发光」既有拍板）。

### U4 全局焦点环
- 全仓 `:focus-visible` **仅 4 条，全在 app.css**：`:494`(.dash-stat-card)、`:511`(.dash-bar-row.clickable)、`:555`(.icon-btn-danger)、`:637`(.btn-del-ghost)。**无全局规则**。
- `outline:none` 抹除点：CSS → `index.css:176/789/864/915/1418`、`app.css:51/158/160/209/228/271/272/281/283/304/315/319/376/565`；内联 → `BomEditor.jsx:15`、`ProcessEditor.jsx:16`、`DrawingLibrary.jsx:55`、`SettingListEditor.jsx:37`、`SizeTable.jsx:494/510/516/522`、`KanbanView.jsx:279`。
- 高频可点但无焦点可见态：侧栏 `.menu-item`（`App.jsx:380-392`，div）、详情 `.tab`（`DetailView.jsx:217-221`）、`.ss-display`（`SmartSelect.jsx:62`）、看板卡片 `.card/.bento-card`（`KanbanView.jsx:176`，最高频）、列表行 `.list-row`（`KanbanView.jsx:559`）、`.drawing-card`、`.cat-item`、`.pdf-upload-zone`（`DetailView.jsx:331`）。
- **唯一正确范例**：`DesignerDashboard.jsx:106-113 / 135-143`（role=button + tabIndex + onKeyDown + :focus-visible）→ 留给 U14 当范式。

### U5 全局 Esc
- 现有 Escape 监听 **3 处，全是“取消行内编辑”，无一处关弹窗**：`PeopleEditor.jsx:109`、`PeopleEditor.jsx:135`、`SettingListEditor.jsx:40`。
- 弹窗/浮层 13 个，**全部无 Esc / 无 role="dialog" / 无焦点陷阱**：ConfirmModal.jsx:12、PdfPickerModal.jsx:37、OperationLogsModal.jsx:37、VersionHistoryModal.jsx:98、NewTaskModal.jsx:80、MeasurementModal.jsx:30（连遮罩关闭都没有）、DrawingLibrary.jsx:385、DrawingLibrary.jsx:455、SizeGroupManager.jsx:30、MeasurementTemplateManager.jsx:151、ExportButton.jsx:60、SmartSelect.jsx:66、DatePicker.jsx:66。
- **PdfThumb 放大预览现状修正**：`PdfThumb.jsx:138-188`（createPortal）。关闭 = ①点遮罩(`:141`) ②点 X(`:164-170`)；**点图片不关**（内层已 stopPropagation `:144/:154`）。⇒ “仅空白/× 关”**已基本满足**，U5 该项**只缺 Esc**。
- 另注：多处遮罩用 `onClick`（拖选文本松手会误关），建议 mousedown+click 双判定：`PdfThumb:141`、`PdfPickerModal:37`、`NewTaskModal:80`、`ExportButton:60`、`DrawingLibrary:385/:455`、`SizeGroupManager:30`、`MeasurementTemplateManager:151`。

### U6 死文件与死节点
- ① `App.css` **确认死文件**（`grep -rn "App.css" src/` 零命中；`main.jsx:3-5` 未引入）。删除**不会**造成 logo 破损（`.logo` 实际由 `app.css:34` 定义）。
- ② 工艺死节点 = `KanbanView.jsx:92` `mk('工艺','','')`（审计写 :81-91，实际函数体 `buildRunNodes()` 在 84-94）。date 恒空 ⇒ status 恒 `pending` ⇒ 渲染灰点（`getNodeIcon` `:15` 用 `--text-4`）。删除后进度节点 **5→4**。
- ③ `SettingsView.jsx:67` = `{ key:'custom', label:'自定义', desc:'深蓝黑自写配色（现有）' }`，与实际的「暖黑底 `--bg #0e0f11` + 香槟金 `--accent #c8a96e`」**不符**（应改「暖黑底 · 香槟金」）。同页 `:68/:69` 文案正确无需改。

---

## B. 批 3（U8–U12）锚点

### U8 状态色 token 化
- **theme.css 无任何状态/语义色令牌**。全部状态色硬编码：JSX 中硬编码 hex 约 **93 处 / 17 文件**（Top：DrawingLibrary.jsx 30、KanbanView.jsx 14、SampleRunList.jsx 9、SizeTable.jsx 8、DesignerDashboard.jsx 7）。
- 语义色实际取值（高频）：
  - 成功/绿：`#4ade80`（KanbanView:13/146/166/184、TaskCard:6、DesignerDashboard:11/24/81、OperationLogsModal:8、DetailView:241、SampleRunList:18/32/48）、`#34d399`（app.css:247/385/628、VersionHistoryModal:201）、`#10b981`（index.css:1363 `em.audit-pass`）、`#22c55e`（SizeTable:104）
  - 危险/红：`#ef4444`（app.css:548/549/555/633/636、index.css:1351/936/420/1100、KanbanView:163、SampleRunList:30）、`#dc2626`（app.css:549/631）、`#b91c1c`（app.css:632）、`#f87171`（App.jsx:282、app.css:248、SampleRunList:32/248、VersionHistoryModal:625/629）、`#f43f5e`（app.css:86/105、index.css:1082、KanbanView:155、SampleRunList:30）、`#fb7185`（index.css:1083、SizeTable:369/372）
  - 警告/琥珀：`#f59e0b`（OperationLogsModal:10、KanbanView:164、ProcessEditor:233、VersionHistoryModal:172）、`#fbbf24`（KanbanView:23、SampleRunList:17、DesignerDashboard:10、NewTaskModal:95/123）、`#eab308`（KanbanView:165）、`#facc15`（app.css:624/630）、`#fb923c`（OperationLogsModal:12、KanbanView:156、SampleRunList:30）
  - 信息/紫：`#a78bfa`（20 处）、`#c4b5fd`（app.css:400、PdfThumb:148、DrawingLibrary:266/288）、`#818cf8`（index.css:7 `--accent-purple`）、`#6366f1`（KanbanView:150/151）、`#f472b6`（DrawingLibrary:23）、`#94a3b8`（DrawingLibrary:39）；青：`#2dd4bf`（OperationLogsModal:11）
- **「彩色底 + `color:var(--text)`」违规点（完整）**：
  - `app.css:548` `.btn-danger{background:#ef4444;color:var(--text)}`
  - `app.css:631` `.btn-danger{background:#dc2626;color:var(--text)}`（**同文件二次定义** `.btn-danger`，与 :548 冲突）
  - `app.css:127` `.bento-overdue-badge{background:rgba(239,68,68,.92);color:var(--text)}`
  - `app.css:391` `.drawing-del:hover{background:rgba(239,68,68,.8);color:var(--text)}`
  - `index.css:1246` `.bento-badge{background:rgba(0,0,0,.65);color:var(--text)}`
  - `KanbanView.jsx:181/184` 复用 `.bento-overdue-badge` 且内联背景 `rgba(245,158,11,.92)`/`rgba(234,179,8,.85)`（前景同 `--text`）
  - `index.css:413` `.drawing-svg-preview svg{background:var(--text)}` 与 `PdfThumb.jsx:120` `background:'var(--text)'` —— **把 `--text` 当“浅色底”用**，light 下 `--text`≈近黑 ⇒ 反相
  - `App.jsx:274-276` 失败横幅（浅色 tint，影响小，登记）
  - light 主题破法：红底 `#ef4444` + 近黑字 ≈2.4:1；`index.css:1246` 黑底+近黑字 ≈不可见。
- 建议 token 命名：`--color-success[-fg|-soft]`、`--color-danger[-fg|-soft|-strong]`、`--color-warn[-fg|-soft]`、`--color-info[-fg|-soft]`、`--color-neutral`、`--color-overdue[-fg]`；run 状态 `--run-waiting/-pattern/-sample/-confirm/-done`。硬规则：**任何彩色底必须配 `--color-<x>-fg` 前景，禁止彩色底使用 `--text`**。

### U9 尺度阶梯 + 按钮收敛
- 按钮类名实测 ≥12（审计所列）+ 审计漏计：`.btn-primary`（index.css:70，**全仓未用=死类**）、`.btn-danger`（app.css:548/631）、`.btn-sort`（index.css:320/809）、`.btn-sm`（index.css:1026）、`.btn-upload-pdf`（app.css:258）、`.btn-icon-sm`（**被 4 处使用但全仓无定义**：DrawingLibrary:282、ExportButton:44、KanbanView:350/358）、`.op-btn`（app.css:103）、`.del-btn`（index.css:409/920）、`.del-btn-mini`（app.css:84）、`.run-del-btn`（app.css:433）、`.run-add-btn`（app.css:462）、`.run-material-btn`（app.css:454）、`.pdf-action-btn`（app.css:252）、`.pdf-enlarge-btn`（app.css:255）、`.drawing-ver-badge`（app.css:376）、`.people-add-role/.people-suggest-btn`（app.css:317/321）、`.tag-del`（app.css:299）、`.dp-nav/.dp-act`（app.css:587/599）、`.run-linked-manage`（app.css:527）、`.theme-option`（app.css:640）、`.btn-glow`（**空规则** app.css:112-113）。
- 关键量值表（padding / height / radius / font-size）见下表；收成 3 类建议：`btn--primary`（.btn-blue/.btn-blue-sm/.btn-add-mini/.btn-add-circle/.btn-upload-pdf）/ `btn--ghost`（.btn-ghost/.btn-ghost-sm/.btn-danger→`.btn--ghost.is-danger`/.btn-mode-toggle/.btn-del-ghost）/ `btn--icon`（.btn-icon/.btn-icon-xs/.icon-btn/.op-btn/.btn-sort/.del-btn）。

| 类 | 文件:行 | padding | height/min-h | radius | font-size |
|---|---|---|---|---|---|
| .btn-blue | app.css:352 | 10px 22px | — | 10 | 14 |
| .btn-blue-sm | app.css:60 | 6px 14px | — | 6 | 12 |
| .btn-ghost | app.css:354 | 10px 20px | — | 10 | 14 |
| .btn-ghost-sm | app.css:260 | 6px 0 | — | 继承 | 12 |
| .btn-icon | app.css:355 / index.css:281 | 6px | — | 8 | icon |
| .btn-icon-xs | app.css:62 | 4px | — | 4 | icon |
| .btn-add-mini | app.css:306(+58) | 0 8px | **32** | 6 | — |
| .btn-add-circle | app.css:73 | — | **36×36** | 50% | — |
| .icon-btn / -danger | app.css:552 / 554 | — | **28×28** | 8 | icon |
| .btn-del-ghost | app.css:636 | 继承 | — | — | — |
| .btn-mode-toggle | index.css:1124 | 6px 12px | — | 6 | 13 |
| .btn-primary | index.css:70 | 10px 20px | — | 8 | inherit |
| .btn-danger(548) | app.css:548 | 10px 22px | — | 10 | 14 |
| .btn-danger(631) | app.css:631 | 7px 14px | — | 8 | 13 |
| .btn-sort | index.css:320 / 809 | 1px / 1px 4px | — | 0 / 3 | icon |
| .btn-sm | index.css:1026 | 6px 12px(!) | — | — | 12(!) |
| .pdf-action-btn | app.css:252 | 0 10px | **30** | 8 | 11 |
| .pdf-enlarge-btn | app.css:255 | 0 18px | **38** | 10 | 13 |
| .op-btn | app.css:103 | 6px | — | 8 | icon |

- `border-radius` 分布（11 种 + 50%）：`6px×36、8px×32、10px×19、12px×17、4px×13、20px×11、50%×7、16px×4、5px×3、0×3、3px×3、2px×1、24px×1` ⇒ 审计「8 档(4/6/8/10/12/16/20/24)」成立，另有杂值 `2/3/5px`。
- 控件高度分布：`min-height:36px×5、height:32px×3、28px×3、30px×2`；icon-btn 28×28、btn-add-circle 36×36、pdf-action 30、pdf-enlarge 38 ⇒ 5 档(28/30/32/36/38)成立。
- 阶梯建议：`--radius-sm:6 / -md:8 / -lg:12 / -pill:999px`；`--h-sm:28 / -md:32 / -lg:36 / -xl:40`；`--fs-xs:11 / -sm:12 / -md:13 / -lg:14`。

### U10 字体本地化
- 远程 `@import`：`index.css:1`（Inter `400;500;600`）、`index.css:2`（DM Mono `ital,wght@0,400;0,500;1,400`）。**无 `@font-face`、无本地字体、无 preconnect**。
- `font-family` 声明仅 5 处：`index.css:16` body Inter；`index.css:27` `.mono` DM Mono；`app.css:2` body Inter；`app.css:4` 表单控件 inherit；`app.css:6` 等宽族 DM Mono（`input[type=number],.tl-date,.bento-style-no,.bento-order-no,.run-order-no em,.bento-node-date`）；`app.css:640` `.theme-option` inherit。**无 `--font-*` 令牌**。
- `index.html` 无字体 link；`:7` title=`golden-shuttle`、`:5` favicon=`/vite.svg`、`:2` lang=`en`（登记为品牌一致性问题，归 U12）。
- **重要缺口**：`font-weight` 实测分布 `700×38、600×26、800×15、900×9、500×5、400×1、950×1`，但 Inter 只下载了 **400/500/600** ⇒ 当前 **700/800/900/950 全是合成加粗（faux bold）**。本地化须补 Inter `600/700/800`（900/950 建议归并到 800）。
- DM Mono 用了 400/500；`@import` 额外含 `1,400`（斜体）但**未见实际使用**（`.text-dim{font-style:italic}` app.css:109 不绑定 DM Mono）→ 可不下斜体省体积。
- `src/assets/` 存在但只有 `src/assets/react.svg`（4126B，**全仓未引用=死资源**）；`src/assets/fonts/` **不存在**；全仓 **无任何 .woff/.woff2/.ttf**。
- 路径建议：
  - A（推荐）：装 `@fontsource/inter`、`@fontsource/dm-mono`（**当前未安装**），`index.css` 顶部改 `@import '@fontsource/inter/400.css'` 等 → Vite 输出 woff2 到 `dist/assets`，**彻底离线**。
  - B：手工拉 woff2 落 `src/assets/fonts/` + 自写 `@font-face`。
  - 体积：Inter latin ≈12–18KB/字重、DM Mono ≈10–14KB ⇒ 合计 **≈70–110KB**（可选用 Inter variable ≈40–60KB 单文件覆盖 400–800）。
- 许可：Inter 与 DM Mono 均 **SIL OFL 1.1**，可随应用分发（保留 OFL 声明），**无风险**。
- 验收：① `grep -rn "fonts.googleapis.com\|fonts.gstatic.com" dist/` = 0；② `dist/assets` 内有 woff2；③ 断网启动无第三方字体请求、`getComputedStyle(body).fontFamily` 命中已加载 Inter；④ 数字列渲染为 DM Mono。

### U11 三主题保留香槟金
- 切换机制：`App.jsx:67-70` `document.documentElement.setAttribute('data-theme', themeMode)`；存储键 **`pm_theme`**（`App.jsx:62-65` 读、`:69` 写，默认 `custom`）；选项 UI `SettingsView.jsx:66-70`。

| 主题 | `--accent` | `--accent-text` | `--accent-soft` | 性格 |
|---|---|---|---|---|
| custom | `#c8a96e` theme.css:28 | `#0e0f11` :29 | `rgba(200,169,110,.12)` :32 | 香槟金（品牌色）|
| dark | `#e6e8eb` theme.css:63 | `#1a1a1a` :64 | `rgba(255,255,255,.1)` :67 | 冷白灰 ⇒ **金消失** |
| light | `#1f2329` theme.css:97 | `#ffffff` :98 | `rgba(0,0,0,.06)` :101 | 近黑 ⇒ **金消失** |

（`--accent-btn` 同步被改：custom `#c8a96e` / dark `#e6e8eb` :65 / light `#1f2329` :99）
- 成因行：**`theme.css:63`（dark）与 `theme.css:97`（light）**。`--accent` 承载全站强调语义（`.tab.active::after` app.css:190、menu 左竖条 app.css:346、`.groupby-ss` app.css:680、`.bento-style-no` index.css:1274、`.bento-order-no` :1316、聚焦环 app.css:494/511/555/637）⇒ 变中性后**品牌识别归零**。
- `theme.css:113` 注释只约束「不要蓝」，**未禁止金色**——当前实现把「不要蓝」误读为「要中性灰」。`@supports not` 回退块（:114-121）只改 `--text`/`--input-bg`，不涉 accent，无干扰。
- 建议：dark `--accent:#c8a96e` / `--accent-text:#141410`；light `--accent:#8a6d2f` / `--accent-text:#fff`（浅底用深一档金保对比度）。

### U12 术语常量表
- **概念文案散布与冲突**：
  - 批次实体同一事物 **4 种叫法**：`KanbanView.jsx:87`（列「版次」）、`:294`、`:319`、`:33`；`DetailView.jsx:306`（「打样批次」）；`SettingsView.jsx:26`（「打样版次库」）；`NewTaskModal.jsx:148`；`SampleRunList.jsx:184`（「版次基本属性」）却 `:373`（「新增打样批次」）；`app.css:416/470/478` 注释 ⇒ **「版次」vs「批次」混用最严重**。
  - 单号：`App.jsx:81`「版单号」vs `SampleRunList.jsx:152`「打样单号」vs `KanbanView.jsx:206`/`TaskCard.jsx:38`「版单：」vs `techPackModel.js:196`。
  - doing 状态：「打版中」vs「进行中」（口径=打版中+样衣中）vs「样衣中」。
  - 待审：「待审版」vs「待审核」vs 旧词「待确认」残留注释。
  - 款/单：「打样单」vs「开发单」（NewTaskModal.jsx:123）vs「单据」（DetailView.jsx:211）vs「款式」。
  - 「部位」全站一致 ✓。
  - 产品名冲突：`index.html:7` title=**golden-shuttle**、`App.jsx:376/394`「PatternMaster / PatternMaster v3.1.0」、Settings/Kanban「PatternMaster Pro」。
- `src/constants/` **不存在**。类常量散落且**同一状态词表在 3 个文件各写一份**：`KanbanView.jsx:19` RUN_STATUS_META、`SampleRunList.jsx:13` RUN_STATUS、`DesignerDashboard.jsx:5` STATUS_META（另有 `:16` RUN_STATUS_RANK）、`NewTaskModal.jsx:6` STATUS_CN、`OperationLogsModal.jsx:6` ACTION_META、`BomEditor.jsx:10/11`、`ProcessEditor.jsx:12`、`DrawingLibrary.jsx:18`、`utils/people.js:3`、`App.jsx:76-105` listColumns。
- 结构建议：`src/constants/terms.js` 导出 `RUN_STATUS`（key→label/color，色值走 U8 令牌）、`PRIORITY`、`AUDIT_STATUS`、`KANBAN_COL`、`BATCH_ENTITY`、`ORDER_NO_LABEL`、`STYLE`、`MEASUREMENT_PART='部位'`、`DRAWING_CATEGORIES`、`SECTIONS`、`BOM_CATEGORIES/UNITS`、`PRODUCT_NAME`。
- 迁移优先级：**P0** RUN_STATUS / STATUS_META / STATUS_CN 三合一 + BATCH_ENTITY 定名；**P1** ORDER_NO_LABEL、PRIORITY、AUDIT_STATUS、DRAWING_CATEGORIES；**P2** SECTIONS、BOM/UNITS、PRODUCT_NAME/favicon/title。

---

## C. 依赖顺序结论

- **无依赖、可最先做**：**U12**（纯新增 `src/constants/terms.js` + docs 术语表，不碰 CSS）；U6③ 文案（`SettingsView.jsx:67` 单行）；U6① 删死文件（`App.css` 零引用）；U10④ 许可/体积决策。
- **建议在 U7 之前独立做**：**U1**、**U2**、**U3**（止血，文件独立）；U4 可先在 `app.css` 尾部加一条全局 `:focus-visible` 作过渡，最终归位 base 层；U5 建议以 U13 `<Modal>` 基座承接，但 **PdfThumb 放大层的 Esc** 可即时补。
- **必须在 U7 之后**：**U8**（状态色 token 落 tokens 层 + 替换硬编码）、**U9**（尺度 token + 改类名）、**U10 的 `@font-face`/`@import` 落点**、**U11**（accent 改值）。
- **与 U7 重叠需协调**：U6① 删 App.css、`.col` 宽度冲突（`index.css:1148-1152` vs `KanbanView.jsx:480`）、`.btn-danger`/`.btn-sort` 重复定义、index.css 与 app.css 双份同名类 ⇒ 交 U7 统一清理，U8/U9 只负责「改名 + token 化」。
- **U11 与 U8 强耦合**：状态色 token 与 `--accent` 同在 `theme.css`，建议同一窗口一起改。

## D. 未命中 / 明确否定（防再猜）

- `src/constants/` 不存在；`src/assets/fonts/` 不存在；全仓无 woff2/ttf。
- `App.css` 零引用（确认死文件）。
- 「placeholder 二次衰减」**仅 `app.css:660` 一处**。
- `.btn-primary`（index.css:70）全仓未使用=死类；`.btn-icon-sm` 被用但无定义；`.btn-glow` 空规则；`src/assets/react.svg` 未被引用。
- `html`/`body` **无** overflow 规则（U1 裁切只来自 `App.jsx:264/265`）。
- 全仓 `:focus-visible` 共 4 条（与审计一致）。
