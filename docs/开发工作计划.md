# 开发工作计划

> 创建：2026-09-09　|　依据：docs/待开发文档.md（需求登记处）
> 用途：承接「待开发文档」中登记的需求，制定开发顺序、实施要点与待拍板决策点；随需求状态变化持续更新。
> 更新：2026-09-09 决策点已全部拍板（用户确认），进入可实施状态。

---

## 一、项目现状摘要（文档阅读结论）

- **项目**：PatternMaster Pro（golden-shuttle）——女装打样管理桌面应用
- **技术栈**：Electron 34（ABI 132）+ Vite 7 + React 19 + Express 5 + better-sqlite3（本地 SQLite）
- **当前分支**：`feature/sample-run-model`（双视角重构分支，main 保持稳定待合并）
- **数据库迁移**：最新 **v17**（v10 批次表+存量合并 / v11 linked_drawing_ids / v12 清理 tasks 旧批次字段 / v13 版师·样衣工拆分 / v14 单号·审核下沉版次 / v15 历史版本表 / v16 尺寸表下沉批次 / v17 版师上移款级）
- **数据现状**：款单 6 张、款式 6 个、版次批次 8 个（26AWW526 外套 V0/V1/V2 为用户真实数据，不可删）
- **已完成**：REQ-001 ~ REQ-017 全部落地；双视角重构 7 阶段全部完成
- **开发纪律（AGENTS.md 强制）**：任何改动必须自测通过再交付；破坏性/变更性交互须有确认步骤；自测走 HTTP fetch（禁止直连 DB）+ Puppeteer E2E（userDataDir 放 %TEMP%）+ 生产构建；每步完成先与用户确认再进入下一步

## 二、待开发项总览

| 编号 | 需求 | 优先级 | 改动范围 | 决策状态 |
|---|---|---|---|---|
| REQ-018 | 版次（打样单）数据模型补全，覆盖样衣制作全流程 | P1 | 迁移 v18 + sampleRuns 服务/路由 + SampleRunList 表单分组重排 | ✅ 已完成（2026-09-09，见 §3.6） |
| REQ-019 | 人员预设中已存在人员姓名可重新编辑 | P1 | PeopleEditor.jsx + 后端改名同步接口 | ✅ 已完成（2026-09-09，见 §4.5） |
| REQ-020 | 统一详情页同类控件尺寸与格式（表单控件规范） | P1 | SampleRunList / SmartSelect / DatePicker + 全局表单样式 | ✅ 已完成（2026-09-09 首轮；2026-09-11 驳回重开二次实施，见 §7.5） |
| REQ-021 | 版次卡片「审版意见/批次备注」下移模块底部 | P2 | SampleRunList 位置调整 | ✅ 已完成（2026-09-11，见 §九） |
| REQ-022 | 修复看板进度计算与实际数据对不上（逾期/节点/版次条口径） | P0 | KanbanView / tasks.cjs attachRuns | ✅ 已完成（2026-09-11，见 §十） |
| REQ-023 | 号型系列「编辑变新增」bug：编辑走更新语义 + 清理重复行 | P1 | SizeGroupManager / api/index.js / size_groups 数据 | ✅ 已完成（2026-09-11，见 §十一） |
| REQ-024 | 看板「全部」视图自适应多列（CSS grid 按视口排 N 列，卡片宽度随列自适应；其余分组视图列宽不变） | P1 | KanbanView 主体三元分支/renderBentoCard 提取 | ✅ 已完成（2026-09-11 重开重做，见 §十四） |
| REQ-025 | 看板「任务状态」分栏口径=最先进未完成批次（不再错分已完结） | P1 | KanbanView / tasks.cjs / sampleRuns.cjs / index.cjs | ✅ 已完成（2026-09-11，见 §十三） |
| REQ-026 | 重写各品类测量部位数据（measurement_templates，以 garment-tech-partner 权威数据为准） | P1 | measurement_templates 表（97 行/11 品类覆盖式重写） | ✅ 已完成（2026-09-11，见 §十五） |
| REQ-027 | 「当前进度」取最新未完成版次（sort_order 最大，修正 REQ-025 状态 rank 口径） | P1 | tasks.cjs / sampleRuns.cjs / KanbanView | ✅ 已完成（2026-09-11，见 §十六） |
| REQ-028 | 看板列标题与卡片同宽（列头 width:100% 继承列宽） | P2 | KanbanView 列头样式 | ✅ 已完成（2026-09-11，见 §十七） |
| REQ-029 | 看板从详情/其他视图切回时自动 loadTasks（无需 F5） | P1 | App.jsx prevView + useEffect | ✅ 已完成（2026-09-11，见 §十八） |

---

## 三、REQ-018 开发计划

### 3.1 需求理解

版次批次需完整覆盖一件样衣制作全流程，按用户拍板分为 5 组字段：
①版次基本属性；②进度；③纸样相关属性；④样衣相关属性；⑤资料。

### 3.2 现状字段对照（2026-09-09 读码核对 SampleRunList.jsx）

| 分组 | 字段 | 现状 | 处理 |
|---|---|---|---|
| ①基本属性 | 单号 | ✅ `order_no`（只读 PO-款号-Vn） | 保留 |
| | 版次 | ✅ `sample_type` | 保留 |
| | 尺码 | ✅ `size` | 保留 |
| | 颜色 | ✅ `sample_color` | 保留 |
| | 打样件数（默认 1 件） | ✅ `sample_count` 默认 1 | 保留 |
| | 审核状态 | ✅ `audit_status` | 保留 |
| | 审版意见 | ✅ `audit_comment`（textarea） | 保留 |
| | 备注 | ✅ `note` | 保留 |
| ②进度 | 当前状态 | ✅ `status` | 保留 |
| | 优先级 | ✅ `priority` | 保留 |
| | 阻塞原因 | ✅ `blocker` | 保留 |
| ③纸样相关 | 版师 | ✅ 款级 `styles.pattern_maker`（REQ-015 已上移） | **不进入批次卡片**（用户拍板：不需要增加） |
| | 纸样完成时间 | ❌ 缺失 | **新增列** `pattern_date` |
| ④样衣相关 | 样衣工 | ✅ `sample_maker` | 保留 |
| | 样衣完成时间 | ✅ = `finish_date`（实际完工，用户拍板：本来就是一个字段） | **不加新列**，文案可保持「实际完工」 |
| | 预期完成时间 | ✅ `expected_date` | 保留 |
| | 面料到库时间 | ✅ `fabric_date` | 保留 |
| | 辅料到库时间 | ❌ 缺失 | **新增列** `accessory_date` |
| ⑤资料 | 尺寸表 | ✅ 批次卡片「尺寸表」入口（锁定版次） | 保留（资料组入口） |
| | 面辅料清单 | ✅ BOM（BomEditor，详情页 Tab） | 保留（资料组入口） |
| | 纸样 | ✅ 图纸资料库（drawings 纸样分类） | 保留（资料组入口） |

**确定要做的**：新增 `pattern_date`（纸样完成时间）、`accessory_date`（辅料到库时间）两列；批次卡片表单按 5 组重排；不加样衣完成时间列、版师不进批次卡片。

### 3.3 决策结论（2026-09-09 用户拍板）

1. **样衣完成时间**：与「实际完工」（finish_date）本来就是同一字段，**不加新列、不改语义**。
2. **版师**：款级字段（styles.pattern_maker，REQ-015 决策），**批次卡片不增加版师**。
3. **表单分组排布**：保持单卡片长表单（页面滚动），卡片内部按 5 组加小标题分区（基本属性 / 进度 / 纸样 / 样衣 / 资料），组内字段顺序按 REQ-018 清单；不做折叠。资料组放三个入口按钮：尺寸表（Ruler 现有入口）、物料清单（跳详情页 BOM Tab）、纸样（跳图纸资料纸样分类）。

### 3.4 实施要点

- **迁移 v18**：`sample_runs` 加 `pattern_date`、`accessory_date`（TEXT，yyyy-MM-dd，可空）；迁移前备份 `server/database.backup_before_v18.sqlite`
- **后端**：`server/db.cjs` 迁移；`server/services/sampleRuns.cjs` PATCH 白名单加新列（RUN_KEYS）；`server/routes/sampleRuns.cjs` 透传（薄适配层无需改）；`attachRuns()` 兼容投影如涉及同步
- **前端**：`SampleRunList.jsx` 批次卡片按 5 组小标题分区重排 + 新增 2 个 DatePicker（复用 REQ-009 DatePicker 组件，全站统一）；资料组三个入口（尺寸表按钮保留 + 物料清单/纸样入口）
- **历史版本（REQ-011）**：versions.cjs 快照含批次字段，确认 buildSnapshot 取列方式（无硬编码列名则新列自动纳入）
- **验证**：迁移后 HTTP 全链路（PATCH 新列读写/投影）+ 浏览器 E2E（新字段展示/录入落库/reload 保持/5 组分区渲染/资料入口可跳转/零 console 错误）+ 生产构建 + 测试数据恢复

### 3.5 联动风险

- 与 REQ-004（款/版次归属）、REQ-005（尺寸表归属版次）、REQ-015（版师上移）的既有决策一致，**不推翻已拍板项**
- 26AWW526 V0/V1/V2 为用户真实数据，迁移只加列不动值，验证后恢复原状

### 3.6 实施结果（2026-09-09 已完成）

- 迁移 v18：sample_runs 加 pattern_date（纸样完成时间）、accessory_date（辅料到库时间）；迁移前已备份 database.backup_before_v18.sqlite
- 后端：sampleRuns.cjs FIELDS 白名单加两列（PATCH 走既有 sanitize/update 链路）
- 前端：SampleRunList 批次卡片按 ①版次基本属性 ②进度 ③纸样相关 ④样衣相关 ⑤资料 五组小标题分区（长表单不折叠）；资料组三个入口（尺寸表/物料清单/纸样，跳详情 Tab）；DetailView 传 onOpenBom/onOpenDrawings；新增 .run-section-title/.run-materials-row/.run-material-btn 样式
- 验证：全新库 v1-v18 迁移完整性 4 项 PASS（v17 与 v18 共存）+ HTTP 新字段链路 8 项 PASS + 浏览器 E2E 28 项 PASS + 生产构建通过（8.7s）；测试数据已恢复（新字段全空，26AWW526 未触碰）
- 过程中修复一处编辑失误：Edit 曾误将 v17 迁移替换为 v18，已恢复 v17 块并追加 v18，全新库测试确认 v1-v18 序列完整

---

## 四、REQ-019 开发计划

### 4.1 需求理解

「人员预设」（PeopleEditor）中已存在人员的姓名目前为纯文本展示、无编辑入口；姓名录入错误或需改名时只能删除重加，且删除仅移除预设、历史引用断关联。期望姓名支持重新编辑（即改即存），重名校验与新增一致；**改名后同步更新全站引用**（用户已拍板）。

### 4.2 现状核对（2026-09-09 读码确认）

- `src/components/settings/PeopleEditor.jsx`：people-row 内姓名为 `<span className="people-name">{p.name}</span>` 纯文本 ✓
- 角色支持 + 添加 / 标签 × 删除、人员支持 × 删除（ConfirmModal 确认）；**唯独姓名无编辑入口** ✓
- 数据 `[{ name, roles }]` 存于 settings；全站人员选择（款式设计师 SmartSelect / 批次版师·样衣工 / 看板「全部分派设计师」筛选）按 **name 字符串**引用联动（README_DEV 已记录的「字段直接存储 vs ID 引用」架构缺陷范畴）

### 4.3 决策结论（2026-09-09 用户拍板）

**改名后同步更新全站引用**——选项 B：
- 改名时同步更新已落库的引用字段：`styles.designer`（款式设计师）、`sample_runs.pattern_maker`（批次版师）、`sample_runs.sample_maker`（批次样衣工）
- 看板「全部分派设计师」筛选、SmartSelect 联动按 name 实时匹配，数据更新后自动一致
- **不回改**：历史版本快照（task_versions 内 JSON，反映当时状态）、操作日志（operation_logs，历史动作记录）——快照/日志语义即历史快照，不做改写

### 4.4 实施要点

- **后端新增改名接口**：`PATCH /api/people/rename`（body: `{ oldName, newName }`），**事务内**执行：
  1. `settings.people`：JSON 数组中 oldName → newName
  2. `styles.designer = oldName` → newName（UPDATE styles）
  3. `sample_runs.pattern_maker = oldName` → newName（UPDATE sample_runs）
  4. `sample_runs.sample_maker = oldName` → newName（UPDATE sample_runs）
  5. 返回受影响记录数（styles / runs 各计），失败回滚
  - 归属：server/services/settings.cjs（或独立 people 服务）+ routes/settings.cjs；IPC 通道（preload/main/client）若需同步注册
- **前端 PeopleEditor.jsx**：
  - 姓名 span 改为可编辑（点击姓名进入编辑态 input，或悬停铅笔按钮，实现时择优）
  - 提交：Enter / blur 保存（调用 rename 接口）；Escape 取消；重名校验与新增一致（`people.some(p => p.name === v)`，与新增同规则）
  - 保存成功 → onChange 刷新 settings + 通知父级 loadTasks 刷新全站（设计师/版师/样衣工列与筛选随之更新）
  - 列表渲染 key 改用稳定索引（当前用 p.name，改名后 key 变化触发重渲染）
  - 角色操作（添加/删除）、人员删除确认（ConfirmModal）不受影响
- **验证**：HTTP 全链路（改名后 settings/styles/runs 三处引用同步 + 事务回滚）+ 浏览器 E2E（点击进入编辑态/改名保存落库/全站设计师·版师·样衣工显示新名/reload 保持/重名阻止/取消还原/角色与删除功能回归/零 console 错误）+ 生产构建 + 测试数据恢复

### 4.5 实施结果（2026-09-09 已完成）

- 后端：`PATCH /api/people/rename`（settings.cjs `renamePerson` 事务）——settings.people + 遗留 designers 兼容数组 + styles.designer + sample_runs.pattern_maker/sample_maker 同步；重名/不存在/空名拒绝、同名幂等；IPC `settings:renamePerson` 同步注册（main.js / preload.js / client.js）
- 前端：PeopleEditor 姓名可点击编辑（Enter/blur 即改即存、Escape 取消、重名校验与新增一致、列表 key 改稳定索引），改名成功 loadSettings+loadTasks 刷新全站；新增 .people-name-input/.people-name-pen 样式
- 验证：HTTP 27 项 PASS + 浏览器 E2E 17 项 PASS + 生产构建通过（9.0s）；测试数据已恢复（白洁/SS26-DR002/MR.Y 与基线一致）

---

## 五、开发顺序建议

| 顺序 | 事项 | 前置条件 |
|---|---|---|
| 1 | **REQ-019**（组件级 + 后端改名接口，无数据迁移） | ✅ 已完成（2026-09-09，commit 1f12b8c） |
| 2 | **REQ-018**（迁移 v18 + 后端 + 表单 5 组重排） | ✅ 已完成（2026-09-09，commit 8efc5ac） |
| 3 | **REQ-020**（表单控件统一规范，建议上调 P1 后实施） | 用户确认优先级与控件规范基线 |
| 4 | 每步：自测（HTTP + Puppeteer E2E + 生产构建）→ 与用户确认 → git commit；REQ 状态同步更新回 docs/待开发文档.md | — |

> 理由：REQ-019 独立、无迁移，可快速交付验证流程；REQ-018 涉及数据迁移与表单大改，紧随其后实施。

---

## 六、开发过程纪律（沿用 AGENTS.md）

1. 改动必须自测通过（真实结果）才能交付，禁止"应该没问题"
2. 数据验证一律走 HTTP fetch（shell node 直连 better-sqlite3 报 ABI 错误，禁止直连）
3. E2E userDataDir 放 %TEMP%（避免 Vite EBUSY 崩溃）；测试脏数据必须清理、用户真实数据恢复原状
4. git add 指定文件，禁止 `git add -A`；不推送测试残留
5. 每完成一步先与用户确认，未确认不擅自进入下一步
6. vite build 必须在真实路径 `D:\dev\golden-shuttle` 下执行（符号链接路径构建报 fileName 错误）

---

## 七、REQ-020 开发计划（表单控件统一规范）

> 状态：开发中（2026-09-09 用户确认上调 P1）

### 7.1 控件规范基线（2026-09-09 制定，落地详情页 .field 系 + SmartSelect + DatePicker + 按钮）

| 维度 | 规范值 | 说明 |
|---|---|---|
| 高度 | `min-height: 36px`（padding 8px 12px） | input/select/textarea/SmartSelect/DatePicker 统一 |
| 边框 | `1px solid var(--border)`；hover `var(--accent-soft-2)` | 全部控件一致 |
| 圆角 | `8px` | 全部控件一致（原 select/input 10px 已收敛） |
| 字体 | `13px`；行高 1.5 | 值字体统一 |
| 占位 | `var(--text-4)`（`::placeholder` 与 .dp-placeholder/.placeholder 一致） | 统一占位色 |
| 聚焦态 | `border-color: var(--accent)` + `box-shadow: 0 0 0 2px var(--accent-soft)` | input/select/textarea 已有；**SmartSelect 打开态、DatePicker 打开态补同款 ring** |
| 宽度 | `.field` 内一律 100%（grid 列分配）；**移除版次 SmartSelect 外包固定 150px** | 宽度策略统一 |
| 上色方式 | 值色走 **CSS 变量/语义类**（`--sel-color` + 类），禁止内联 style 直接写色值 | 状态/审核/阻塞收敛 |
| textarea | 统一 `height: 110px; resize: vertical; line-height: 1.6`，去内联样式 | 审版意见用类 |

### 7.2 实施清单

1. app.css `.field input/select/textarea`：padding 11px 14px→8px 12px、圆角 10px→8px、字体 14px→13px、补 `min-height:36px`；textarea 补 line-height 1.6
2. 删除 `.run-grid input/select` 独立覆盖（12.5px/6px 8px），统一走 .field 基线
3. `input::placeholder, textarea::placeholder { color: var(--text-4) }`
4. SmartSelect：`.ss-display` 对齐基线 + `.ss-display.open` 聚焦 ring（SmartSelect.jsx 加 open 类）
5. DatePicker：`.dp-input.open` 聚焦 ring（DatePicker.jsx 加 open 类）
6. SampleRunList：移除版次 SmartSelect 外包 150px；审版意见去内联样式；状态/审核/阻塞 select 色值改 CSS 变量（`style={{ '--sel-color': ... }}` + 类应用）
7. 按钮：尺寸表/物料清单/纸样已统一为 .run-material-btn（REQ-018 已收敛）；删除批次 icon-btn-danger 符合 REQ-007 基线
8. 全站同型评估：SmartSelect/DatePicker/.field 为共用类，统一后详情页各 Tab、新建打样单弹窗、设置页同型控件同步受益；其余页面若有独立控件样式留待后续轮次评估（本次以详情页批次表单+共用控件收敛为准）

### 7.3 验证

浏览器 E2E（详情页批次表单控件几何一致性断言：input/select/DatePicker/SmartSelect 高度一致、圆角一致、聚焦 ring 存在；版次下拉不再固定 150px；状态/审核/阻塞值色经 CSS 变量生效；审版意见无内联 style；占位色统一）+ 生产构建 + 数据恢复（无数据变更，仅样式）

### 7.4 实施结果（2026-09-09 已完成）

- 规范基线落地（见 §7.1）：`.field input/select/textarea` 统一 padding 8px 12px / 圆角 8px / 字体 13px / min-height 36px / hover accent-soft-2 / 聚焦 accent ring；删 `.run-grid` 独立覆盖；补 `::placeholder` 占位色 var(--text-4)；SmartSelect/DatePicker 打开态补聚焦 ring（`.ss-display.open` / `.dp-input.open`）
- SampleRunList：版次 SmartSelect 去外包 150px（`.run-card-title .smart-select { min-width: 140px }` 统一宽度策略）；审版意见 textarea 去内联 style；状态/审核/阻塞值色改 CSS 变量 `--sel-color` + 语义类
- 验证：浏览器 E2E 15 项 PASS + 视觉截图确认 + 生产构建通过（8.7s）；无数据变更

### 7.5 驳回重开二次实施（2026-09-11 已完成）

> 用户视觉裁决（_req_screenshot_020d.png）：✅ 绿框＝版次 SmartSelect（深色圆角触发框 + 面板带「手动输入自定义值」行与「预设选项」分区）为标准外观；❌ 红框＝打版状态/审核状态/阻塞原因三处原生 select 及其黑色原生下拉面板。用户确认：「手动输入自定义值」行不是重点，关键是外观统一。

- **SmartSelect 组件增强**：支持 `{key,label}` 枚举选项（显示 label、提交 key）、`allowCustom` prop（枚举 false 隐藏自定义输入行）、`className`/`style` 透传（`--sel-color` 注入根节点、子级 CSS 继承取值）
- **SampleRunList 五处原生 select → SmartSelect**：打版状态（RUN_STATUS {key,label}，卡片头 run-status-ss min-width 96px）/ 尺码（getSizeList）/ 优先级（PRIORITIES）/ 审核状态（AUDIT_STATUSES）/ 阻塞原因（BLOCKERS {key,label}），全部 allowCustom=false（枚举保持语义），值色走 `--sel-color`（状态按状态色/审核按审核色/阻塞非 none 红）
- **app.css**：删除废弃 `.run-status-select`/`.audit-select`/`.blocker-select`；`.ss-display` 支持 `--sel-color`；面板沿用既有深色绿框样式
- 验证：E2E 25 项 PASS（0 原生 select、7 个 SmartSelect 触发框几何一致、枚举面板无自定义输入行、版次面板保留、当前值高亮、值色生效、改状态落库并恢复、零 console 错误）+ 视觉截图目检（无黑色原生面板）+ 生产构建通过（9.9s）；测试数据无残留

---

## 八、order_no 服务端只读化补强（2026-09-09 核查产出）

> 背景：全面核查时发现 tasks.order_no 全空引发"前端拼接"误判，查证后确认单号本就由后端按规则生成（v14 迁移 + `generateOrderNo`，权威在 sample_runs.order_no，tasks 兼容列设计清空），前端仅读取显示。但存在两个真实隐患需补强。

### 8.1 隐患与决策

| 隐患 | 说明 | 决策 |
|---|---|---|
| 单号可被客户端覆盖 | create/update 白名单含 `order_no`，`if (!data.order_no)` 才自动生成——客户端可显式传值改坏单号规则 | **服务端只读化**：create 忽略客户端传入、始终 `generateOrderNo`；order_no 移出 FIELDS 白名单，update 天然不可改 |
| 无唯一约束兜底 | `generateOrderNo` 读 max+1，无索引防线 | **迁移 v19**：`CREATE UNIQUE INDEX idx_sample_runs_order_no ON sample_runs(order_no) WHERE order_no != ''`（部分索引排除空串，幂等） |

删除批次不重排（V 编号留洞）为有意设计（保持历史单号稳定），不做改动。

### 8.2 实施

- server/services/sampleRuns.cjs：FIELDS 移除 `'order_no'`；create 中 `data.order_no = generateOrderNo(taskId)`（忽略客户端传入）
- server/db.cjs：迁移 v19 加唯一索引

### 8.3 验证（全部通过）

- HTTP 全链路 10 项 PASS：create 传 `PO-HACK-V99` 被忽略、按规则生成递增单号（PO-SS26-DR002-V1）；PATCH 改 order_no 无效；其他字段正常更新（回归）；删除测试批次后单号集恢复基线
- 直连 SQL：重复单号插入被唯一索引拒绝且无数据残留（事务回滚）
- 全新库迁移 v1-v19 完整（迁移序列/唯一索引/关键列/tasks.order_no 兼容列保留）
- 浏览器 E2E：UI 新建批次 → 卡片显示后端生成单号（PO-SS26-DR002-V1）→ 数据恢复
- 无历史数据变更（现库 10 批次单号本就唯一，索引直接可建）

---

## 九、REQ-021 开发记录（2026-09-11 已完成）

> 版次卡片内「审版意见」与「批次备注」下移至模块底部（P2，纯位置调整无数据变更）。

- **改动**：SampleRunList 中「审版意见（各版次独立）」textarea 与「批次备注」input 整体移出「版次基本属性」组，下移至「资料」按钮行（尺寸表/物料清单/纸样）之后、「绑定资料版本」区之前，作为卡片收尾字段；字段绑定与自动保存逻辑未动
- **验证**：随 REQ-020 重开 E2E 顺序断言 PASS（资料按钮 < 审版意见 < 批次备注 < 绑定资料版本）+ 视觉截图目检 + 生产构建通过

---

## 十、REQ-022 开发记录（2026-09-11 已完成）

> 看板「逾期情况」进度计算与实际数据对不上（P0）。用户截图实证：SS26-DR002 显示「逾期 13 天」但其批次日期在 3-8 月；SS26-TS003 显示「逾期 185 天」而进度更快的卡只逾期 13 天；底部节点版师/样衣/工艺全 "--"。

### 10.1 根因核查（读码 + 真实数据）

- **逾期基准错误**：`getOverdueInfo` 仅读款级 `task.expected_date`（历史遗留字段，批次化后无任何反写、也无 UI 维护入口）——DR002 款级 08-29（→13 天）、TS003 款级 03-10（→185 天）；而批次级真实预期：DR002 V0=09-11（今天）、TS003 V1 生产样=03-25
- **底部节点脱节**：渲染 `task.progress_nodes`（用户手填工作动态，详情页右栏维护），日期/负责人与批次真实数据无关（DR002 样衣手填空，批次实际完工 08-27）
- 款级 `status` 已有批次同步（sampleRuns.cjs syncTaskStatus），无问题

### 10.2 口径拍板（依据既有决策链推导）

1. **进度权威 = 最先进批次**（延续 REQ-003③；deriveStyleStatus/findTopRun 同口径）
2. **交期基准**：最先进批次 `expected_date`（批次表单可维护）→ 该款全部批次中最早的预期 → 款级 `expected_date` 仅旧数据兜底 → 无则不算逾期
3. **完成判定**：款级完结（done/completed）或最先进批次已完成（done）→ 不算逾期
4. **底部进度节点 = 最先进批次真实数据**：配料=面料到库 fabric_date / 跟版=任务开始 start_date / 版师=纸样完成 pattern_date（负责人=款级版师）/ 样衣=实际完工 finish_date（负责人=批次样衣工）/ 工艺=无对应批次字段；工作动态仍在详情页完整可编辑

### 10.3 实施

- **后端** `server/services/tasks.cjs` `attachRuns`：投影 `top_run` 最先进批次摘要（expected_date/fabric_date/start_date/pattern_date/finish_date/sample_maker/audit_status/status/sample_type），与 derived_status 同源 findTopRun，保证前后端口径唯一
- **前端** `src/components/task/KanbanView.jsx`：`getOverdueInfo` 重写（基准三级回退 + 完成判定 + 返回 due）；逾期/today/soon 角标 title 标注「最先进批次」；底部节点 `buildRunNodes` 固定 5 节点批次派生（负责人入 title）；看板分组列自动随新口径

### 10.4 验证

- HTTP：top_run 投影正确（DR002=头版样 sample_making 预期 09-11 / TS003=生产样 pattern_making 预期 03-25）
- 浏览器 E2E 13/13 PASS：DR002 角标「今日到期」（不再 13 天）、TS003「⚠ 逾期 170 天」（不再 185 天）、节点=批次真实数据（配料 03-01/跟版 03-08/样衣 08-27）、TS003 生产样无日期节点全 --、节点固定 5 个、版次条无回归、DR002 归「今日到期」列、TS003 归「已逾期」列、零 console 错误
- 视觉截图目检：逾期分组三列（已逾期 1/今日到期 2/3 天内到期）、TS003 逾期 170 天、DR002 与 AW26-PA004 今日到期、节点与版次条口径一致
- 生产构建通过（9.9s）；测试无数据写入；后端改动已重启服务（PMProDev）再验证

---

## 十一、REQ-023 开发记录（2026-09-11 已完成）

> 号型规格系列「编辑变新增」bug（P1）：编辑保存后新增同名行，列表不断产生重复。

- **根因**：前端 saveSizeGroups 永远 POST /api/size-groups（后端 POST 忽略请求体 id 走 INSERT）；PATCH 路由/update() 已存在但前端从未调用；组件层 setEditing(g) 带 id 无 bug
- **修复**：api/index.js 新增 updateSizeGroup(id,data)（PATCH）；SizeGroupManager.save 按 editing.id 区分（有 id 走 PATCH 更新原系列、无 id 才 POST 新增）
- **清理重复**：数据库核对 size_groups 6 行，id6「女装」与 id5「女装号型（上装）」size_list 完全相同且无任何分类绑定（settings.categories 绑定 连衣裙→2/夹克→1/外套→1/风衣→2/衬衣→2/半裙→1/裤子→4）→ 判定编辑改名误新增的重复行，DELETE id6，其余 5 行不动
- **验证**：E2E 编辑改名→列表行数不变（5→5）且原行更新→改回恢复；删除 id6 后列表 5 行；新增/编辑/删除三路径回归通过

## 十二、REQ-024 开发记录（2026-09-11 已完成）

> 看板「关注点：全部」单栏视图兜底（P1）：不做分组、筛选仍叠加，先让用户完整看到所有款。

- **实施**：KanbanView getActiveCols 新增 all 分支（单列「全部」）；分组过滤 all 直接放行；下拉新增「关注点：全部」选项
- **验证**：E2E 单栏 1 列 + 全部 6 款 + 截图目检 + 生产构建通过

## 十三、REQ-025 开发记录（2026-09-11 已完成）

> 看板「任务状态」分栏把进行中款错分进「已完结」（P1）：26AWW526（头版样样衣中+复版一待配料）、235-6（头版样打版中）误入已完结。

- **根因**：分栏读款级存储字段 task.status（旧口径=最先进批次含已完成批次→胚样已完成即判 done）；前端 normalizeStatus 又兜底脏值
- **口径修正**：完成=该款**全部批次均已完成**，否则取**未完成批次中最先进**（延续 REQ-022 最先进批次权威、排除已完成批次）
- **实施**：①后端 deriveStyleStatus 改「active=未完成批次，无则 done」；findTopRun 改「未完成批次中最先进，全 done 回退首个批次」——top_run=当前进行中批次（26AWW526 版单 PO-V0→PO-V1 头版样，符合 REQ-004「当前进行中批次」投影意图）；②syncTaskStatus 同步新口径；③index.cjs 启动 recalcAllTaskStatus() 存量重算（幂等）：26AWW526/235-6 done→doing 归位；④前端新增 derivedCol(t)（derived_status→todo/doing/done），状态分栏与列表状态列改实时推导，不再读 task.status；getOverdueInfo 完成判定同步 derived_status==='done'
- **验证**：HTTP 6 款款级状态重算归位 + E2E（26AWW526/235-6 归打版中列、AW26-JK001 在已完结、零 console 错误）+ 截图目检（打版中 5 / 已完结 1）+ 生产构建通过（9.7s）

## 十四、REQ-024 开发记录（2026-09-11 重开重做·已完成）

> 单列实现浪费横向空间（docs/_req_screenshot_026.png：卡片挤在左侧 400px 单列、右侧约 60% 空白）→「全部」视图改自适应多列。

- **实施**：看板主体改三元分支——kanbanGroupBy==='all' 时卡片直排 CSS grid `repeat(auto-fill, minmax(360px,1fr))`（卡片宽度随列数自适应，不固定单列）；其余分组视图维持 400px 列宽；renderBentoCard 提取为共用函数（全部/分组视图同卡渲染）；列头 width 100%（REQ-028）
- **验证**：E2E 断言容器 display=grid、6 卡、首行 3 列（1440px 视口）、卡宽 443px + 截图目检（3 列铺满）+ 生产构建通过（9.66s）

## 十五、REQ-026 开发记录（2026-09-11 已完成）

> 重写 measurement_templates：原 10 行质量差（外套/连衣裙等实际品类无预设、公差格式混乱）→ 按技能权威数据覆盖式重写。

- **数据源**：garment-tech-partner references/01-06（品类部位）、08（测量方法/公差基线）、11/14（档差）
- **实施**：97 行/11 品类（上装11/外套9/夹克9/风衣9/连衣裙11/裤子10/半裙7/衬衣8/针织上装7/针织下装9/毛衫7）；code 01.. 规范；公差基线 衣长/袖长/肩宽/裤长/裙长 ±0.5、胸围 ±1、腰围 ±0.8、其余 ±0.3；档差 胸/腰/下摆 4、肩宽/袖长/袖口/领围 1、前浪 0.5、后浪 0.7、横裆 2.2、中裆 2、脚口 2/1.8/1.6；electron node 模式事务写库，备份 server/database.backup_before_req026.sqlite；按用户拍板不处理现有款数据
- **验证**：GET /api/measurement-templates 全表 97 行、?category=外套 9 行正确返回
- **修复（2026-09-11 用户报告「下装未写入」）**：数据在库但品类命名三处错位——设置页目录读 settings.measurementCategories（原 5 项含历史品类「下装」）、模板权威品类为裤子/半裙/针织下装；存量下装款 AW26-PA004 category=「下装」预设取空。①settings.measurementCategories ← 11 模板品类；②settings.categories 补齐 上装/针织上装/针织下装/毛衫（含 size_group 绑定）；③styles「下装」→「裤子」；④PA004 复版二 run6 size_data 补裤子预设 10 行。验证：设置页 11 品类目录 + 裤子 10/半裙 7/针织下装 9 行可见、新建表单品类 11 项、PA004 尺寸表 10 行裤子部位，E2E 14 项全过

## 十六、REQ-027 开发记录（2026-09-11 已完成）

> 修正 REQ-025 口径错误：「当前进度」应以**最新版次**为准（开新版次=从头重做），非状态 rank 最高。

- **实施**：tasks.cjs deriveStyleStatus 改 active=status!=='done' 取 sort_order 最大者的 status（无 active→done）；findTopRun 改 active 空回退全部 runs 取 sort_order 最大；sampleRuns.cjs syncTaskStatus 同步（SELECT status, sort_order）
- **验证**：重启（PMProDev）后 HTTP 6 款归位（26AWW526→todo/复版一 waiting_material/版单 PO-26AWW526-V2；TS003→todo/生产样 waiting_material；235-6→doing 保持；AW26-JK001→done 保持）+ E2E 分栏断言（待处理2：26AWW526/TS003；打版中3：235-6/DR002/PA004；已完结1：AW26-JK001，无串栏）+ 截图目检 + 生产构建通过

## 十七、REQ-028 开发记录（2026-09-11 已完成）

> 列标题宽度与卡片不一致（docs/_req_screenshot_028.png：卡片探出列头右侧）。

- **实施**：列头 width:400 → width:'100%' 继承列宽（与卡片同源同宽）；.col-body 去 padding-right；与 REQ-024 重开一并落地
- **验证**：E2E 断言各列列头宽度=卡片宽度（600=600×3）+ 截图目检（列头与卡片右缘对齐）+ 生产构建通过

## 十八、REQ-029 开发记录（2026-09-11 已完成）

> 看板数据不随返回刷新：详情页改数据返回仍旧值，需 F5。

- **根因**：onBack 只 setView('kanban') 不 loadTasks（App.jsx）；即改即存已落库但看板内存 tasks 不重拉
- **实施**：App.jsx 新增 prevView=useRef(view) + useEffect（view==='kanban' 且 prevView!=='kanban' 时 loadTasks()），插在初始加载 useEffect 后，覆盖详情页返回及其他视图切回路径；不改即改即存
- **验证**：E2E 详情页生产样预期交期 07-10→09-30→返回看板 TS003 角标「逾期 63 天」即时消失（未 F5）→改回 07-10 恢复 + 零 console 错误


## 十九、REQ-030 文案改词与优先级体系替换（2026-09-11）

- **需求**：①版次状态改词「待配料」→「待安排」、「待确认」→「待审版」（存储 key 不变，仅展示 label）；②优先级体系 低/中/高/紧急 → S/A/B/C 四级替换，全系统所有出现处统一，存量迁移（紧急→S、高→A、中→B、低→C）。
- **实施**：
  - 前端改词：KanbanView / SampleRunList / DesignerDashboard 三处 RUN_STATUS_META label；仪表盘统计卡「待安排/未开始」「待审版」。
  - 优先级四级：KanbanView PRIO_RANK S/A/B/C（taskTopPriority 默认 B）、优先级分组列 S/A/B/C（S 红 / A 橙 / B 品牌色 / C 中性）、过滤栏与新建弹窗选项 C/B/A/S、「关注点：紧急程度」→「关注点：优先级」、批次卡「优先」值色 prioColor、prio 类映射 S→high/A→mid/B·C→low。
  - 后端：sampleRuns.cjs 默认优先级 中→B；db.cjs 迁移 v20 存量映射 sample_runs + tasks（紧急→S、高→A、中→B、低→C）。
  - 存量实测：sample_runs 高1→A、低1→C、紧急1→S、中7→B；tasks 款级 紧急→S（235-6）、高→A（JK001）、其余 B。
- **验证**：E2E 13/13 PASS（无旧词/过滤栏/分组列 S/A 卡/面板选项/新建弹窗/仪表盘改词/零 console 错误）+ 截图目检（优先级分组 S/A/B 列、批次优先级面板）+ 生产构建通过（10.04s）。
- **说明**：期间一次构建报错系 JSX 属性行尾/模板串内插行注释非法（DesignerDashboard 统计卡行、NewTaskModal option 后注释等），已全部移除改为纯 JS 语句行尾注释；优先级映射未单独询问用户，按文档建议（紧急→S、高→A、中→B、低→C）实施。


## 二十、REQ-031 版次状态筛选 + REQ-032 卡片最小宽度 500px（2026-09-11）

- **REQ-031 版次状态筛选**：过滤栏优先级后新增「全部版次状态」下拉（待安排/打版中/样衣中/待审版/已完成，RUN_STATUS_META key+label 生成）；filterTasks 存在性过滤（任一版次 status 匹配即放行，与分栏口径解耦）；与搜索/分类/版次/设计师/优先级叠加。验证：选「打版中」仅剩 235-6/AW26-PA004 2 卡（不含全完成的 JK001），清空恢复 6 卡。
- **REQ-032 卡片最小宽度 500px**：全部视图 grid minmax(360→500px,1fr)；分组视图列宽 400→500。验证：1440px 视口全部视图 6 卡实测 676px、分组 3 列实测 600px，款号/品牌无截断。
- **验证**：E2E 11/11 PASS + 截图目检（全部视图 3 列 676px、版次状态筛选 2 卡）+ 生产构建通过（9.87s），commit 见本轮。
